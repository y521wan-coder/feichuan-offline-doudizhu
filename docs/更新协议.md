# 独立更新协议

- 更新服务：`https://update.327802521.xyz`。
- 产品标识：`feichuan_offline_doudizhu`。
- 游戏只通过独立进程启动“飞船单机斗地主更新器.exe”，不直接发起网络请求。
- 自动检查默认启用且每天最多一次；支持关闭和手动启动。
- 下载必须使用 HTTPS，并同时验证 SHA-256 与开发者数字签名。
- 失败、断网或取消不能终止或破坏游戏；应用替换必须支持失败回滚。
- 更新器不得读取或上传存档、手牌、统计、回放、设置和日志。
- 第一版仅支持 Windows。
