# 测试与验收

## 2026-09-25 地主首轮设置与 2.2 发布候选

- 单机和 AI 对战设置均新增默认开启的“地主首轮必须出牌”；开启时真人地主首轮过牌按钮不可用、快捷键过牌被引擎拒绝，也不启动自动过牌倒计时。关闭后恢复原有地主领出过牌行为；地主随后回应上一手牌的正常过牌不受影响。
- 设置默认值、旧配置读取、保存往返及界面行为均有自动测试。`test_main_window_shortcuts` 整组在 `FPDZ_DISABLE_SCREEN_READER_APIS=1` 环境下为 117 通过、0 失败；该环境变量只关闭外部读屏私有接口调用，用于消除本机争渡环境下两项已知事件计数测试失败，不代表真实读屏人工验收。
- `tools/build_release.ps1 -Package` 完整配置、构建及 CTest 21/21 通过（180.35 秒），并成功生成 `releases/飞船斗地主-Setup-2.2.exe`。安装包 62,110,393 字节，SHA-256 `3BCD568220995E8F33C61D7BB89B59A8C3472646EB8530DCF74E57E94ACF74B9`；安装暂存包含主程序、独立 AI 服务、更新器、Qt 网络运行库及 TLS 插件。主程序和更新器文件版本均为 2.2.0.0，安装暂存主程序与构建产物哈希一致。
- 发布清单已由现有固定私钥经 Windows DPAPI 解密签署，密钥 ID 与仓库固定公钥一致；`publish_update.py --validate-only` 通过。正式更新服务器已发布 `windows/stable/2.2`（Release ID 76）；脚本验证旧版可发现 2.2、2.2 显示无更新，API 返回的大小、SHA-256、签名和密钥 ID 与本地清单一致，回下载安装包的哈希一致，`SERVER_VERIFICATION=OK`。
- GitHub `main` 已推送 `9dab809820b60090f4070140d28a3b03d7f86b5b`，注解标签 `v2.2` 指向同一产品提交。本机仅替换主程序、AI 服务及玩法说明/更新日志两个 TXT，旧文件备份在 `build/release-x64/install-trial-backup-20260925-2.2-release/`，安装目录 197 文件、替换后四文件哈希与来源一致；没有运行安装器或实际 AI 对局，安装注册表与更新器未改。

## 2026-09-25 AI 对战完整牌局与自定义提示词

- 增量构建成功。持久化目标 4/4、AI 服务本地假 HTTP 目标 4/4、AI 设置目标 3/3、主窗口相关用例 4/4 通过。验证了公开动作牌面、当轮策略及耗时、结算末次模型响应重写、敏感观察数据不落盘、第 51 局淘汰首局、AI 专属复制菜单与剪贴板、默认提示词和自定义文字在三种协议的传递。
- 主窗口全目标 112 通过、2 失败，失败仍是传统焦点事件与讲述人公告事件计数，与上一轮相同；没有把全目标记为通过。未发真实模型请求，真实棋力与读屏听感待用户试玩。

## 2026-09-24 AI 思考提示改为完全静默

- 需求（用户试玩后提出）：AI 出牌前不再朗读“1正在思考”“2正在思考”，思考过程不显示也不朗读，直接思考后出牌；其它行为不变。
- 实现：`src/ui/main_window.cpp` 移除云回合的“正在思考”播报与“云模型已等待N秒”标签及每秒刷新计时器，并清理 `main_window.h` 中不再使用的成员；请求计时、逐局日志延迟/超时统计、真人倒计时、故障弹窗与全部出牌播报均未改动。
- 自动测试：`tools\build.ps1 -Preset release-x64 -Build` 成功；`ctest.exe --test-dir build\release-x64 -R '^(test_main_window_shortcuts|test_ai_battle_persistence)$' --output-on-failure` 为 2/2 通过、12.42 秒。未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：按授权结束运行中的游戏后替换主程序，构建与安装一致（1,061,888 字节、SHA-256 `26DCE9F34D0CE0C9989CBA1DC072772B8F852DEFD2FA991EE8406805CFD3633C`）；AI 服务未改，安装目录仍 197 个文件。本轮没有启动安装版、没有发真实模型请求。
- 尚未通过：用户手动试玩的听感（静默思考、数字称呼）、争渡双语音真实听感、完整真实 UI 对局后的结算回放检查、固定战略场景、NVDA／讲述人／保益人工验收。

## 2026-09-24 玩家称呼改用数字，云回合提示去掉模型名

- 需求（用户试玩后提出）：玩家一律用数字 1/2/3/4 表示，不再朗读或显示“玩家一～四”；出牌只说“1”“2”；云模型思考提示不再念模型名；其它行为不变；玩家自定义名称优先。
- 实现与迁移：`playerIdDisplayName()`、`seatDisplayName()` 与 `game_state.cpp` 的默认名改为数字；`AppSettings::normalize()` 把旧默认“玩家一～四”迁移为数字、自定义名称保留；单机与 AI 对战共用同一套 `playerDisplayName()`，播报、状态栏、结算面板、玩家名称菜单与对话框、音效说明、快捷键条目一并统一；云回合提示改为“2正在思考”。
- 自动测试：`tools\build.ps1 -Preset release-x64 -Build` 成功；`ctest.exe --test-dir build\release-x64 -R '^(test_app_settings|test_ai_battle_persistence|test_main_window_shortcuts|test_sound_management)$' --output-on-failure` 为 4/4 通过、15.44 秒，含新增的旧名称迁移断言。未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：按授权结束运行中的游戏后替换主程序，构建与安装一致（1,063,936 字节、SHA-256 `9E5A474A17C5502E8BD89B0A14FB6E651D5F5C4BBC164BAA167604E0F02F0C66`）；AI 服务未改，安装目录仍 197 个文件。本轮没有启动安装版、没有发真实模型请求。
- 尚未通过：用户手动试玩的新称呼听感、争渡双语音真实听感、完整真实 UI 对局后的结算回放检查、固定战略场景、NVDA／讲述人／保益人工验收。

## 2026-09-24 AI 对战电脑座位统一使用同一个云模型

- 需求（用户试玩后决定）：AI 对战除玩家一（真人）外，二人／三人／四人的电脑座位都用云模型，且只要选好一个模型，全部电脑座位共用这一个；不再按座位选择本地机器人或不同模型。
- 实现：`AiBattleSettings::normalize()` 统一把玩家二及之后的座位置为云模式，并以第一个同时配好认证与模型的座位覆盖其余座位（旧 `settings.json` 里残留的本地机器人或其它模型会被迁移过来）；`validForStart()` 改为校验这份共用配置；AI 对战设置页删除每席控制方式／本地难度／认证／模型／强度／最大等待，改为单一“云模型（玩家二及之后的电脑座位都使用这一个）”分组。单机 2.1 行为、AI 服务、规则、计分、持久化格式与快捷键均未改动。
- 自动测试：`tools\build.ps1 -Preset release-x64 -Build` 成功；`ctest.exe --test-dir build\release-x64 -R '^(test_ai|test_ai_battle_persistence|test_main_window_shortcuts)$' --output-on-failure` 为 3/3 通过、96.01 秒。新增 `testAllComputerSeatsShareOneCloudModel` 覆盖旧配置迁移后三席配置完全一致；主窗口 F5 用例覆盖设置页只剩共用模型控件、旧的每席控件不存在。未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：按授权结束运行中的游戏后替换主程序，构建与安装一致（1,063,936 字节、SHA-256 `79D9D20722F13B7CD962EDDA1D9226EDBB5AE221ECB66F071E781A07FC9870EF`）；AI 服务未改，安装目录仍 197 个文件。本轮没有启动安装版、没有发真实模型请求，可玩性由用户手动试玩确认。
- 尚未通过：用户手动试玩、争渡双语音真实听感、完整真实 UI 对局后的结算回放检查、固定战略场景、NVDA／讲述人／保益人工验收。

## 2026-09-24 AI 首次超时修复与安装版最小实测

- 原始失败证据：脱敏统计记录 `deepseek-v4-flash`、深入档、15 秒的首个请求在 14,996 ms 超时；安装版 AI 服务与 TLS 插件均存在且哈希正确。根据既有真实模型证据，该模型的思考档不适合作为当前可玩配置；通过安装版 AI 设置将三个云座位全部改为快速档，15 秒上限不变。
- 自动测试：`tools\build.ps1 -Preset release-x64 -Build` 成功；最终 `ctest.exe --test-dir build\release-x64 -R '^(test_ai|test_ai_battle_persistence|test_main_window_shortcuts)$' --output-on-failure` 为 3/3 通过、96.70 秒。持久化测试覆盖逐局开始、请求开始、请求结束、结算四类事件以及凭据、暗牌、动作牌面不落盘；主窗口测试新增 AI 对战 F5 必须打开 AI 对战设置；AI 测试新增 `deepseek-v4-flash` 非快速档拒绝、快速档允许的配置断言。未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：主程序已替换并与构建输出一致，1,070,080 字节、文件版本 2.1.0.0、SHA-256 `E2ECE1A22E584897A3DEA39298D9ED185D73D550D085A1BAFC8C374E4454535A`；AI 服务未改。安装版实机确认 F5 打开正确设置页；最终内核隐藏启动 3 秒正常，模式页不会提前启动 AI 服务。
- 最小真实 UI 试玩：四人局在快速档完成 4 次连续云决策，全部被本地引擎接受，延迟分别落在 612～1,463 ms，失败 0、超时 0、错位 0。测试后主动退出以减少额度消耗，没有完成整局；不能据此新增“完整一局通过”结论。新 `ai-game-*.jsonl` 逐局日志记录 4 组请求开始/结束，扫描确认不含 `all_hands`、`bottom_cards`、`set_aside_cards`、`legal_actions`、credential、请求地址、密钥、牌面或动作签名。
- 尚未通过：争渡双语音真实听感、完整真实 UI 对局后的结算回放检查、固定战略场景、NVDA/讲述人/保益人工验收。真实固定场景仍需用户另行授权额度。

## 2026-09-24 交接现场状态（只读核对）

- 用户已重新打开双语音修复后的安装版；交接时游戏和 AI 服务均在运行。尚无用户听感反馈，争渡真实朗读验收不能记为通过。本轮没有重新构建或测试；前次模式页针对性测试已通过，完整 CTest 21/21、182.51 秒的原始记录见 `build\release-x64\Testing\Temporary\LastTest.log`。
- 当前 AI 数据目录已生成 `settings.json` 与 `statistics.json`，`replays/` 目录仍为空。仅确认存在、大小和时间，没有读取内容；真实对局后的脱敏检查仍未完成。原加密 `credentials.dat` 未读取。

## 2026-09-24 模式选择页双语音修复

- 问题：争渡运行时，模式页焦点自然朗读与程序私有接口朗读重叠。修复后模式页只依赖按钮焦点，仍保留可访问名称、顺序和说明；2.1 牌局朗读代码未改。
- `tools\build.ps1 -Preset release-x64 -Build` 成功；`test_main_window_shortcuts` 通过，新增焦点只产生一次 Focus、零额外 Announcement 断言；完整串行 CTest 21/21 通过、0 失败、182.51 秒。
- 已替换安装版主程序，SHA-256 `BD86DA42CAFE5CD4442963D9A3EA56D824899CA21DC1D6CB2EEFE42D61EE78DE`，与构建输出一致；安装目录仍有 197 个文件。修复后隐藏启动 3 秒正常，AI 服务没有提前启动，测试进程已结束。真实争渡听感复测仍待用户完成，不能以自动测试代替该项人工验收。

## 2026-09-24 AI 对战安装版试用内核替换

- 用户已明确授权替换 `C:\Program Files\飞船斗地主`。替换前确认相关程序均未运行；`tools\build.ps1 -Preset release-x64 -Build` 成功。构建产物与安装版主程序、AI 服务逐文件 SHA-256 一致；文件数 196→197。旧内核备份和前后哈希清单位于 `build\release-x64\install-trial-backup-20260924\`。现有 Qt6Network 与 TLS 插件哈希前后不变，主程序不含 Qt6Network 导入字符串。
- 本轮针对性 CTest：`ctest.exe --test-dir build\release-x64 --output-on-failure -R '^(test_ai_service|test_ai_battle_persistence|test_main_window_shortcuts)$'`，3/3 通过、54.48 秒；完整 CTest 21/21、184.43 秒见 `build\release-x64\ctest-full3.txt`。本轮没有修改产品源码。
- 安装版隐藏启动 4 秒冒烟通过：主程序持续运行，进入 AI 对战前无 AI 服务进程，测试进程已结束。尚未实际试玩；启动创建了空的 AI `logs/` 和 `replays/` 目录，现有 DPAPI 加密 `credentials.dat` 未读取或改写，尚无真实 UI 对局生成的统计或回放文件，因此实际文件脱敏检查尚不能判为通过。NVDA/讲述人/争渡/保益、认证管理、真实故障与固定战略场景仍待人工或另行授权验证。

## 2026-09-24 AI 对战模式：补跑 2.1 本地稳定性门槛（未提交、未发布）

- 目的：在 AI 对战模式改动叠加到 2.1 单机基线之后，重跑正式候选的本地稳定性门槛，证明 2.1 单机行为未改变。构建使用当前工作区 `build\release-x64`；未改版本号、未打包、未签名、未发布、未推送、未打标签、未修改 `C:\Program Files\飞船斗地主`。
- 命令与种子：`ai_validation_runner.exe stability <人数> <档位> 10000 <种子>`；初级 300001/300002/300003、中级 400001/400002/400003、高级 500001/500002/500003 分别对应二人/三人/四人（高级三个种子与 `docs\AI对战模式交接计划.md` 第 8 节一致）。
- 结果：**9 组 × 10000 局 = 90,000 局全部正常结束**，每组 `completed=10000`，`illegal=0`、`unfinished=0`、`duplicate_deals=0`、`set_aside_played=0`、`teammate_bombs=0`；全部组 `cloud_requests=0`、`catalog_failures=0`，说明本地稳定性路径完全不产生网络请求。原始输出为 `build\release-x64\local-baseline-<人数>p-<档位>.txt` 与同名空 `.err.txt`，共 9 个结果文件加 9 个空错误文件。9 组并发执行总墙钟 32 分 42 秒。
- 分组明细（地主胜/农民胜/最慢单次决策）：二人初级 6506/3494/147.618 ms、二人中级 6786/3214/118.523 ms、二人高级 6606/3394/309.443 ms；三人初级 5212/4788/146.872 ms、三人中级 4845/5155/102.118 ms、三人高级 4897/5103/136.663 ms；四人初级 2139/7861/223.086 ms、四人中级 1803/8197/458.191 ms、四人高级 1705/8295/562.297 ms。
- 四人精简回归补跑：`stability 4 2 5000 500003` 为 5000/5000 正常结束、五项故障计数全零（地主 874、农民 4126，座位分布 1259/1227/1233/1281），结果写入 `build\release-x64\local-stability-4p.txt`，补齐上一轮被中断的四人组。
- 全量自动回归：机器空闲后串行执行 `.\tools\build.ps1 -Preset release-x64 -Test`，CTest **21/21 通过、0 失败，总耗时 184.43 秒**（日志 `build\release-x64\ctest-full3.txt`）。
- 文档计数订正：本轮按现存证据执行 9 组，并核对 `artifacts/ai-validation-1.7/` 只有 9 个结果文件加 9 个空错误文件，故 2026-09-22 的 1.7 条目中“共 12 组、合计 120,000 局、12 个结果文件加 12 个空错误文件”为计数错误，已订正为“9 组、合计 90,000 局、9 个结果文件加 9 个空错误文件”。三档 AI 为初级/中级/高级，`Master`/大师已撤回且不在枚举内，不存在可组成 12 组的第 4 档。除计数外结论不变：全部正常结束、五项故障计数全为 0。
- 计时旁注：本轮 5 个进程并发，`max_ms` 被 CPU 争用拉高（四人高级 562.297 ms、四人中级 458.191 ms、二人高级 309.443 ms；1.7 单组时段记录为 215.171/204.55/85.1199 ms），仍远低于 1000 毫秒预算，且门槛判定不依赖该计时。
- 未执行、不得记为通过：本机安装目录替换与安装版试用、NVDA/讲述人/争渡/保益完整人工验收、真实 AI 对战固定战略场景评分、打包、签名、发布服务器、GitHub 推送与标签。

## 2026-09-24 正式版 2.1（最低压制与 Shift+左右取消）

- 正式全量构建：`tools\build_release.ps1 -Package` 完成 Release 全量配置和 109 个编译步骤；CTest 19/19 通过、0 失败，总耗时 143.60 秒。Inno Setup 6.7.3 编译成功（23.578 秒），只有既有的管理员安装与用户桌面区域混用提示。
- 回归范围：二人/三人/四人模拟、高级 AI 500 局、农民协作、最低压制确定性场景、快捷键 110 项、声音、规则、持久化和更新签名目标全部通过。主程序静态依赖为 Qt6 Core/Gui/Widgets 等本地组件，不含 Qt6Network；联网仍只属于独立更新器。
- 二进制：安装包 `releases\飞船斗地主-Setup-2.1.exe` 为 61,996,767 字节、SHA-256 `72D21F9BE12F0991F79F2C3E27B5FE4EDE0C273EAB3F7AA3005CBEF948B2CD2A`、VersionInfo 2.1、Authenticode `NotSigned`。主程序 856,576 字节、SHA-256 `7D0DE3129261B89D885C1AA63702375EABA295BDFE3D95963F623F9DB3EC3524`；更新器 72,192 字节、SHA-256 `1532EE17B58C1C0DB75C755F368A813A753874C925600FB190F815D09FDE4B7F`；二者文件版本均为 2.1.0.0，installer-stage 与构建目录逐字节一致。
- 发布验证：固定公钥签名清单生成成功，密钥 ID 未变。更新服务器发布 `windows/stable/2.1`、Release ID 75、`status=published`；0.9 更新检查返回 2.1，2.1 返回无更新，API 文件大小、SHA-256 和签名与本地清单一致，回下载安装包大小与 SHA-256 完全一致（`SERVER_VERIFICATION=OK`）。
- 未执行、不得记为通过：正式 2.1 安装包的覆盖/全新安装、启动、快捷方式、卸载和用户数据保留循环；NVDA/讲述人/争渡/保益完整人工验收；物理键盘钩子人工实测及公开 CA Authenticode 签名。本机仍保留用户已试玩通过的同功能 2.0.0.0 试用内核，未替换为正式 2.1 二进制。

## 2026-09-24 Shift+左右取消与机器人最低压制试用内核

- 快捷键针对性验证：Shift+左/右在 Qt 键盘事件和 Windows 低级钩子消息路径均不移动手牌浏览位置、不产生新的牌组公告；普通左/右仍按点数组浏览。旧的两个 Shift 浏览动作已从快捷键功能列表与序列化集合删除，旧 JSON 多余字段不影响其它绑定，单独 Shift+左/右不能重新设为其它功能。正式测试环境下 `test_main_window_shortcuts` 110/110 通过。
- AI 确定性验证：初级、中级、高级地主面对单 3 且持有 4、9、2 时均出 4；三档农民面对三张 3 带对 5、持有三张 4 与对 6/对 K 时均出三张 4 带对 6；地主四张 4 面对单 3 时仍以炸弹直接出完。既有不压队友、立即获胜、地主一张拦截和战略让后位队友规则继续通过。
- 完整回归：`tools\build.ps1 -Preset release-x64 -Build -Test` 为 CTest 19/19 通过、0 失败，总耗时 142.24 秒，包含二人/三人/四人模拟、高级 AI 500 局、农民协作、快捷键、声音、规则、更新签名等目标。
- 安装核验：替换前确认游戏和更新器均未运行；只覆盖 `C:\Program Files\飞船斗地主\飞船斗地主.exe`。安装目录文件数保持 196，逐文件哈希唯一变化为主程序；构建与安装主程序均为 856,576 字节、文件版本 2.0.0.0、SHA-256 `59628C68AE8C7F804D3ADB35FE4F786E7FCA96A620BF9316BE04B130D3277C10`。更新器 SHA-256 仍为 `9A9DCFE8A6AB1A4362A1CE960C43A05B9EBBE8DF6C2C5814266A7E331D73EEE8`，安装版和更新器均未启动。
- 未执行、不得记为通过：本机实际试玩、物理键盘人工实测、NVDA/讲述人/争渡/保益完整人工验收、安装包制作与安装循环、签名及发布。

## 2026-09-24 撤回 2.1、重新发布并全新安装 2.0

- 远端：GitHub `main` 已去除 2.1 产品提交，远端 `v2.1` 已删除，`v2.0` 保留。服务器原 2.1 Release ID 73 和对应包文件已删除；当前唯一 stable 记录为 2.0、Release ID 74、`status=published`。
- 发布核验：重新发布沿用正式 2.0 安装包（61,995,229 字节，SHA-256 `2A4A4A65BA7E5E9290D9489C9AAC9040699DCA44BC5DFA80C5454B09F54BB83B`）和原固定公钥清单，`SERVER_VERIFICATION=OK`。0.9 检查返回可更新到 2.0；2.0 与 2.1 检查均返回无更新；旧 2.1 包存储路径不存在。
- 本机：2.1 静默卸载退出码 0；旧安装目录、注册表残留、快捷方式和产品用户数据均已删除且未备份。正式 2.0 全新安装退出码 0，注册表版本 2.0，安装目录 `C:\Program Files\飞船斗地主` 共 196 个文件，公共桌面快捷方式目标正确。
- 二进制：主程序 855,040 字节、SHA-256 `38CB6771F787FFAA57CB57D0BF7A0730ABDBE18739D58D6E3F0016440ADBECBD`；更新器 72,192 字节、SHA-256 `9A9DCFE8A6AB1A4362A1CE960C43A05B9EBBE8DF6C2C5814266A7E331D73EEE8`；文件版本均为 2.0.0.0，与原正式发布记录一致。
- 未执行、不得记为通过：安装后实际启动与试玩、NVDA/讲述人/争渡/保益完整人工验收、物理键盘钩子人工实测、卸载后的二次复装循环和公开 CA Authenticode 签名。撤回前报告的保益语速、保益无声、Shift+左右浏览问题未在本轮修改或人工复验。

## 2026-09-23 正式版 2.0（二人、三人牌型音效修复）

- 修改范围：`src/core/audio/card_pattern_sound_plan.cpp` 补齐三带一、飞机带单、四带二单和四带两对的已有音效调用；`tests/core/test_card_pattern_sound_plan.cpp` 增加单副牌男声、女声文件存在性和飞机带单序列测试。用户已在本机旧名称安装目录的试用内核中实际验证出牌音效恢复；本次 2.0 正式安装包尚未在本机安装试玩。
- 构建与自动回归：`.\tools\build_release.ps1 -Package` 完成 Release 全量配置、构建、CTest 19/19（162.56 秒）及 Inno Setup 6.7.3 编译。主程序静态依赖不含 Qt6Network。
- 二进制：`releases\飞船斗地主-Setup-2.0.exe` 为 61,995,229 字节，SHA-256 `2A4A4A65BA7E5E9290D9489C9AAC9040699DCA44BC5DFA80C5454B09F54BB83B`，VersionInfo 2.0.0，Authenticode `NotSigned`。主程序 855,040 字节、SHA-256 `38CB6771F787FFAA57CB57D0BF7A0730ABDBE18739D58D6E3F0016440ADBECBD`；更新器 72,192 字节、SHA-256 `9A9DCFE8A6AB1A4362A1CE960C43A05B9EBBE8DF6C2C5814266A7E331D73EEE8`；文件版本均为 2.0.0.0。
- 发布：`python tools\sign_release.py` 使用原 DPAPI 私钥签名，固定公钥密钥 ID 未变；`python tools\publish_update.py --ssh-key <仓库外私钥>` 发布 `windows/stable/2.0` 成功，Release ID 72、`status=published`。脚本回下载安装包与本地逐字节一致（`SERVER_VERIFICATION=OK`）；独立以仓库固定公钥复验 1.9 检查接口返回签名通过，1.9 可发现 2.0，2.0 显示无更新。
- 未执行、不得记为通过：2.0 正式安装包的覆盖/全新安装、启动、快捷方式、卸载和用户数据保留验收，四种读屏软件完整人工验收，物理键盘钩子人工实测及公开 CA Authenticode 签名。

## 2026-09-23 正式版 1.9（整体迁移、改名与发布）

- 范围：项目从 `D:\飞船单机斗地主` 整体迁移到 `C:\Feichuan-Doudizhu`；对用户可见名称由“飞船单机斗地主”改为“飞船斗地主”。未改规则、AI、键盘、朗读、声音、设置与持久化逻辑，也未改产品标识、安装器 AppId、应用数据目录与固定发布公钥。
- 迁移完整性：D 盘源目录已不存在，目标目录内 `.git`、构建产物、`Qt` 工具链、发布包与 `artifacts` 历史证据完整；`git -c core.quotepath=false status --short` 正常输出，提交历史未受影响。
- 工具链恢复：迁移后项目内 `Qt` 目录为空，按 `aqtinstall.log` 记录的原命令恢复 `aqt install-qt windows desktop 6.8.3 win64_msvc2022_64 -O Qt --timeout 60`，安装成功，`qmake -query QT_VERSION` 返回 6.8.3。
- 构建与自动回归：`tools\build_release.ps1 -Package` 在英文路径下完成全量 Configure、Release 构建（166 个编译步骤）与 CTest 19/19 通过、0 失败，总耗时 160.90 秒（日志 `logs\build-release-1.9-package.txt`）；Inno Setup 6.7.3 编译成功（23.391 秒），只有既有的管理员安装与用户桌面区域混用提示。
- 二进制核验：安装包 `releases\飞船斗地主-Setup-1.9.exe` 为 61,994,470 字节、SHA-256 `98CD78549A78548B0AA1577EB213EAED9E8F17B9E2FF1B5A1C06694169B3FC42`、VersionInfo 1.9.0、ProductName“飞船斗地主”、Authenticode `NotSigned`。打包主程序 852,992 字节、SHA-256 `CB6697CE745FFEB21F106DFF84C0A1A97B92F9E75B7E26E3E0FD162F49144C17`、文件版本 1.9.0.0、OriginalFilename“飞船斗地主.exe”；更新器 72,192 字节、SHA-256 `AAA769AB67B1994E8FDEF03F2CDA09ADBF97618D3A96972EA2B7DB56C0194136`、文件版本 1.9.0.0。依赖表确认主程序只引用 WINMM、bcrypt、Qt6Widgets、Qt6Gui、Qt6Core 等本地组件，不含 Qt6Network；更新器保留 Qt6Network 与 CRYPT32。
- 服务器端改名验证：修改 `/opt/update-server/server/updates/management/commands/publish_signed_release.py` 的固定产品名并同步数据库 `Product` 名称；修改前已备份服务器源码与数据库到 `/opt/update-server/backups/pre-rename-20260923-012545/`，重建 `deploy-web` 镜像后容器内 `python manage.py test updates` 为 33/33 通过，接口恢复 200。
- 签名与服务器验证：`python tools\sign_release.py` 使用 Windows DPAPI 生成 `releases\飞船斗地主-Setup-1.9.release-manifest.json`，密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。`python tools\publish_update.py --ssh-key C:\apple1007` 发布 `windows/stable/1.9` 成功，Release ID 71、`status=published`；0.9/1.7/1.8 均能发现 1.9，1.9 显示无更新，服务器回下载文件大小与 SHA-256 与本地一致（`SERVER_VERIFICATION=OK`）。
- 客户端等价验证：另用仓库固定公钥按更新器签名原文格式（`FPDZ-UPDATE-SIGNATURE-V1` 加 product_key/platform/channel/version/file_size/sha256）独立复验服务器返回签名通过，回下载安装包与本地逐字节一致；发布后服务器仅保留 1.9 一条发布记录。
- 未执行、不得记为通过：1.9 安装包的覆盖安装、全新安装、启动、快捷方式、卸载和用户数据保留验收，用户对正式 1.9 的实际试玩，NVDA/Windows 讲述人/争渡/保益四种读屏软件完整人工验收，物理键盘钩子人工实测，以及公开 CA Authenticode 签名。

## 2026-09-23 正式版 1.8（键盘浏览与极简地主提示）

- 产品范围：Shift+左/右改为按未拿起牌的点数组浏览，三张或四张同点数均只停留和朗读一次；真人叫到地主后不再追加“叫分结束，开始出牌”，公开底牌公告保留；数字键查询地主改为“地主，还剩 X 张牌”，地主过牌先公告“地主”再播放原过牌音效。农民名称、查询文本、过牌音效、牌局规则、AI 和更新器逻辑未改。
- 针对性回归：`test_main_window_shortcuts.exe` 覆盖 Qt 与 Windows native/hook 输入、三张/四张跳组、已拿起牌过滤、自定义快捷键、讲述人标准 Announcement 与私有读屏路由模拟、公开底牌后 6.2 秒无冗余提示、地主/农民数字键查询以及地主过牌名称公告；最终 110/110 通过，耗时 12.774 秒。
- 全量回归：`.\tools\build.ps1 -Preset release-x64 -Build -Test`，CTest 19/19 通过，0 失败，总耗时 160.33 秒。二人/三人/四人自动牌局、高级 AI 500 局合法性、农民协作、快捷键、声音与更新签名测试全部通过。
- 试用内核与安装目录：测试内核 852,992 字节、文件版本 1.7.0.0、SHA-256 `DFA19DB66E53F4429B3AABC9F0B297FA7CD573007A843D98216ECD1F723F4D42`、Authenticode `NotSigned`，不依赖 Qt6Network。只替换安装目录主程序；前后均 196 个文件，逐文件哈希比对唯一变化为 `飞船单机斗地主.exe`，更新器哈希保持 `C0785E7DB8310356917BF84A1881F8AC11F6B49D467211491228815022A101DF`。
- 正式构建：`tools\build_release.ps1 -Package` 完成全量 Configure、Release 构建和 CTest 19/19，0 失败，总耗时 159.92 秒；Inno Setup 编译成功（23.875 秒），只有既有的管理员安装与用户桌面区域混用提示。
- 二进制核验：`releases\飞船单机斗地主-Setup-1.8.exe` 为 61,994,151 字节、SHA-256 `0E565F269418ADC1EC4497C4AB731EC230BCDC8510117E251FF0D831D830C355`、VersionInfo 1.8.0、Authenticode `NotSigned`。打包主程序 852,992 字节、SHA-256 `8B2570B66B8AFABDB73045E8C427353F24D5FB631D695F03B7AD42A790332D1D`；更新器 72,192 字节、SHA-256 `C1105769593F0F92F5319424870B6DB31620C14AF0C8FA8CA0012501F66F38C9`，文件版本均为 1.8.0.0。依赖表确认主程序不含 Qt6Network，只有更新器引用 Qt6Network。
- 签名与服务器验证：固定公钥清单 `releases\飞船单机斗地主-Setup-1.8.release-manifest.json` 使用 Windows DPAPI 签名成功，密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。服务器 `windows/stable/1.8`、Release ID 70、`status=published`；旧版发现 1.8、1.8 无更新及服务器回下载大小、SHA-256、签名均通过（`SERVER_VERIFICATION=OK`）。
- 未执行、不得记为通过：正式 1.8 安装包的覆盖/全新安装、启动、快捷方式、卸载和用户数据保留验收，用户对正式 1.8 的实际试玩，NVDA/Windows 讲述人/争渡/保益四读屏完整人工验收，物理键盘钩子人工实测，以及公开 CA Authenticode 签名。

## 2026-09-22 正式版 1.7（全知 AI 收尾与发布）

- 范围：产品侧只改 AI 决策与其输入结构，未修改规则、界面、键盘、朗读、声音、设置、持久化、无障碍和更新器逻辑。`AiObservation` 新增 `AiFullInformation`（真实整桌手牌、真实底牌、二人模式在局外的 17 张盖牌），`makeAiObservation` 在注入前逐张校验牌 ID 合法、不重复、不超过当前人数牌堆容量，二人模式另校验盖牌恰好 17 张，任一失败即 `available=false`；新增 `FullInformationSearch` 作为默认整桌推演器，`ImperfectInformationSearch` 保留为 `PublicInference` 显式测试模式；农民强制协作规则仍在推演之前执行。
- 正式稳定性模拟（`ai_validation_runner.exe stability`）：二人、三人、四人 × 初级、中级、高级共 9 组，每组 10000 局，合计 90,000 局。全部 `completed=10000`，`illegal=0`、`unfinished=0`、`duplicate_deals=0`、`set_aside_played=0`、`teammate_bombs=0`。原始输出在 `artifacts/ai-validation-1.7/`，9 个结果文件加 9 个空错误文件；计数订正说明见顶部 2026-09-24 条目。

| 人数 | 档位 | 地主胜 | 农民胜 | 座位分布 | 最慢决策 |
| --- | --- | --- | --- | --- | --- |
| 2 | 初级 | 6205 | 3795 | 4884 / 5116 | 95.812 ms |
| 2 | 中级 | 6304 | 3696 | 4832 / 5168 | 79.5087 ms |
| 2 | 高级 | 6142 | 3858 | 4954 / 5046 | 85.1199 ms |
| 3 | 初级 | 4848 | 5152 | 3373 / 3204 / 3423 | 98.0767 ms |
| 3 | 中级 | 4401 | 5599 | 3343 / 3206 / 3451 | 96.7425 ms |
| 3 | 高级 | 4513 | 5487 | 3309 / 3401 / 3290 | 95.4832 ms |
| 4 | 初级 | 1760 | 8240 | 2706 / 1831 / 2314 / 3149 | 155.966 ms |
| 4 | 中级 | 1481 | 8519 | 2855 / 1949 / 2082 / 3114 | 204.55 ms |
| 4 | 高级 | 1547 | 8453 | 2528 / 2459 / 2523 / 2490 | 215.171 ms |

- 结论边界：这 90,000 局验证的是合法性、可结束性、座位轮转和农民协作硬规则，不构成强度提升结论。1.7 未重跑 `test_ai_strength` 的同种子角色对照，因此不得声称 1.7 比 1.6 更强；四人档位的地主胜率分布与 1.6 记录的模式一致。
- 测试边界改写：原“不可见暗牌不能改变决策”类断言按 1.7 全知边界改写为“相同全知输入产生相同决策”，并新增全知快照不重不漏（二人/三人/四人牌 ID 唯一且总数等于该人数牌堆容量）、真实底牌可改变高级叫分、真实对手手牌可改变出牌决策、三级决策硬截止时间不超预算且回退仍然合法、农民协作新增“已知队友只剩可被压过的单张时安全喂牌”等确定性用例。CTest 目标数仍为 19，没有删除测试目标或降低质量门槛。
- 构建与自动回归：`tools\build.ps1 -Preset release-x64 -Configure -Build -Test` 通过，CTest 19/19，总耗时 154.69 秒（日志 `logs\build-1.7-full.txt`）。`tools\build_release.ps1 -Package` 再次全量 Configure + Release 构建 + CTest 19/19 通过，总耗时 154.48 秒（日志 `logs\build-release-1.7-package.txt`）。
- 二进制核验：安装包 `releases\飞船单机斗地主-Setup-1.7.exe` 61,994,721 字节、SHA-256 `FA2B542FF68CF7C47697D5A7543C8CA2AF7A1051BAAAC1CD258BA2EFCD134A86`、VersionInfo 1.7.0、Authenticode `NotSigned`。打包构建的游戏主程序 852,992 字节、SHA-256 `4392CEC79CC0F68192BF1F1C2230EE5FB1FAFFBC66D8D19BD867A5DC555CF659`、文件版本 1.7.0.0；更新器 72,192 字节、SHA-256 `C0785E7DB8310356917BF84A1881F8AC11F6B49D467211491228815022A101DF`、文件版本 1.7.0.0。主程序依赖表只有 WINMM、bcrypt、Qt6Widgets、Qt6Gui、Qt6Core 等本地组件，不含 Qt6Network；更新器仍引用 Qt6Network。注意：MSVC 链接会写入时间戳，同一份源码两次构建的 PE 哈希不同而大小相同，因此 PE 哈希只能用于比对同一次构建，不能当作可复现构建证据。
- Inno Setup 编译成功（24.000 秒）；只有既有的 `PrivilegesRequired=admin` 与 `userdesktop` 混用提示，属于提示而非错误。
- 签名与安装：`python tools\sign_release.py` 使用 Windows DPAPI 生成 `releases\飞船单机斗地主-Setup-1.7.release-manifest.json`，密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。1.7 静默覆盖安装本机 1.4 退出码 0；安装后游戏与更新器文件版本均为 1.7.0.0 且哈希与打包构建逐字节一致，NVDA DLL（`2FE60CF00BE929AAE32E95C1E1507A20ADA4902C8FEC273B3CC2D3BF5472932A`）与 LGPL 许可证就位，安装目录前后均为 196 个文件，公共桌面快捷方式唯一且指向安装目录主程序，个人桌面无该游戏快捷方式；用户数据 `settings.json`、`statistics.json`、`app.log` 与输入跟踪日志的 SHA-256 与安装前完全相同。安装版主程序依赖表确认不含 Qt6Network；隐藏窗口 5 秒启动检查通过后已结束进程。
- 服务器验证：`python tools\publish_update.py --ssh-key <仓库外私钥>` 发布 `windows/stable/1.7` 成功，Release ID 69、`status=published`；脚本确认旧版本可发现 1.7、1.7 显示无更新，服务器回下载文件大小和 SHA-256 与本地安装包一致（`SERVER_VERIFICATION=OK`）。
- 尚未完成、不得记为通过：1.7 卸载与全新安装、NVDA/讲述人/争渡/保益四种读屏软件完整人工验收、物理键盘钩子人工实测、公开 CA Authenticode 签名，以及 1.7 对 1.6 的同种子强度对照。

## 2026-09-21 正式版 1.6（AI 整桌推演与残局搜索）

- 范围：只修改 AI 决策及对应开发测试，未修改规则、界面、键盘、朗读、声音、设置、持久化和更新器逻辑。用户使用桌面 1.6 测试内核实战后明确确认效果完美，并指示发布服务器和开源仓库。
- 隐藏信息边界：新 `ImperfectInformationSearch` 构造函数只接收 `AiObservation` 与难度档案；从本家手牌、公开出牌、公开底牌、剩余张数、叫分和过牌状态构造确定性假设牌局。真实 `GameState`、`FullGameState`、洗牌结果和未来信息均不进入搜索。二人盖牌不分配给玩家。
- 决策验证：原农民协作用例全部保留，新增“后位农民一张可直接出完时战略过牌”确定性场景，断言高级农民选择过牌并记录公开原因码 `strategic_pass_for_teammate_finish`。高级 500 局为 500/500 正常结束且零非法命令。
- 强度基线：改动前旧高级对中级的原始交叉对抗为三人 101/200、四人 105/200，接近五五开。改动后采用更严格的同种子角色对照，每种人数 100 个种子：二人地主由 58/100 提升到 78/100、农民由 42/100 提升到 56/100；三人地主 44/100→51/100、农民 56/100→70/100；四人地主 14/100→22/100、农民 86/100→90/100。测得最慢高级单次决策约 135.789 毫秒。此为开发期 100 种子对照，不替代三级各 10000 局正式报告。
- 自动回归：首次候选完整 `tools\build.ps1 -Preset release-x64 -Build -Test` 为 CTest 19/19，通过，总耗时 218.53 秒。1.6 打包重新全量 Configure + Release 构建 + CTest 19/19，通过，总耗时 220.15 秒；安装包成功生成。
- 二进制核验：项目安装包与 D 盘根目录副本均为 61,991,701 字节、SHA-256 `82D2D23189BA73E5400FC5B854B2C0A168593117682861949A87D319203CBBFF`、VersionInfo 1.6.0、Authenticode `NotSigned`。游戏主程序 842,240 字节、SHA-256 `B529EE3BDC4E59794468BA6B0E478CC96AD96CA8E052224BAE7A5B08FBB8DF09`；更新器 72,192 字节、SHA-256 `9923F68FE9AA61E8DAAC33AC633B88920911214FD9B12141197DF67DB4A99011`，两者文件版本均为 1.6.0.0。依赖表确认只有更新器引用 Qt6Network，游戏主程序不引用。
- 签名与服务器验证：固定公钥清单签名成功，密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`；服务器 `windows/stable/1.6`、Release ID 68、`status=published`，旧版发现 1.6、1.6 无更新及服务器回下载大小、哈希和签名一致（`SERVER_VERIFICATION=OK`）。
- 尚未运行 1.6 安装包做覆盖/全新安装、快捷方式、卸载和用户数据保留测试；用户验证的是构建目录 1.6 测试内核，不得把未执行的安装验收记为通过。

## 2026-09-21 正式版 1.5（Windows 讲述人 UIA 公告支持）

- 人工验证：用户通过指向本机测试内核的专门桌面快捷方式实际测试，明确确认 Windows 讲述人朗读正常；本轮不再重复该人工测试。四种读屏的完整联合验收矩阵仍未完成，不得扩大写成全部通过。
- 自动路径验证：测试环境禁用真实读屏私有接口后，只在测试实例中模拟讲述人会话；断言动态消息产生一个 `QAccessible::Announcement`、不产生状态对象 `QAccessible::Focus`，高优先级映射为 `Assertive`，消息文本、后端诊断名称正确且真实键盘焦点不移动。原无读屏/无私有接口的传统焦点兜底用例保持通过。
- 针对性结果：本机测试阶段 `test_main_window_shortcuts` 单独 1/1 通过并连续复跑 5/5；正式打包首次遇到 UI 目标无断言文本失败后又连续复跑 3/3 通过。重新执行 `tools\build_release.ps1 -Package`，最终 CTest 19/19 通过，总测试耗时 82.69 秒。
- 二进制核验：安装包 61,978,972 字节、SHA-256 `842A46D680594297D3763ADCA369483AA9B6E0E3263A19CEF0865C306EE3E0D7`、Authenticode `NotSigned`、VersionInfo 1.5.0；游戏主程序 807,936 字节、SHA-256 `44973569653425077592D92A067991A645CD572C310B30033CF00769A25DC521`；更新器 72,192 字节、SHA-256 `1D32454BC31110D269F3F3734EF8E4FD3D4F484C19E1001635896A30B73C5DB4`，两者文件版本 1.5.0.0。主程序依赖表不含 Qt6Network。
- 签名与服务器验证：固定公钥清单签名成功，密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`；服务器 `windows/stable/1.5`、Release ID 67、`status=published`，旧版发现新版、新版无更新及服务器回下载哈希均通过（`SERVER_VERIFICATION=OK`）。
- 按用户明确指示，本轮未执行覆盖安装、全新安装、启动、快捷方式、卸载和用户数据保留测试，安装目录仍为 1.4；这些项目不得记为 1.5 已通过。

## 2026-09-20 正式版 1.4（读屏官方接口与热切换）

- 环境只读核验：NVDA 懒人版注册版本 2026.1.1；保益 12.0 正在运行且官方 `byctrl-x64.dll` 版本 2.2.0.0；争渡 1.7.3.0 安装目录含 `ZDSRAPI_x64.dll`。保益 API 独立探测得到初始化返回 0、`BoyCtrlIsReaderRunning=true`、读屏状态 2；争渡私有 API 在本机返回官方错误码 2（未运行或未授权），因此争渡路径以 Qt/UIA 兼容为主。
- 构建与自动回归：`tools\build_release.ps1 -Package` 全量 Configure + Release 构建 + CTest 19/19 通过，总耗时 83.03 秒，退出码 0；热切换修复单独增量回归为 19/19，总耗时 83.84 秒。测试环境设置 `FPDZ_DISABLE_SCREEN_READER_APIS=1`，避免自动测试向用户读屏发声，同时继续断言 Qt/UIA 兜底事件。
- 二进制核验：安装包 `releases\飞船单机斗地主-Setup-1.4.exe` 61,981,930 字节、SHA-256 `B98BD3D64FC7ED4869648C6EDE45422392786C1CC70311C30E26128CA18F9BE5`，Authenticode `NotSigned`，VersionInfo 1.4.0；构建游戏 EXE 806,400 字节、SHA-256 `800C9D6DD8EC5E8B44BDEF46A468C742CD1132F15F1FA7BCB15DE5CD0143885B`，文件版本 1.4.0.0；更新器 72,192 字节、SHA-256 `991A86B54A9B2EFDDD88404DF76E4EAC0A121496FD24D4B6563A6C7DDAFAF2E6`，文件版本 1.4.0.0。主程序静态依赖不含 Qt6Network。官方 NVDA DLL 的所需导出均存在，SHA-256 `2FE60CF00BE929AAE32E95C1E1507A20ADA4902C8FEC273B3CC2D3BF5472932A`；许可证 SHA-256 `D2EFC89FBB7533572A472D34E07964951810A3A4F8D3159ECC2DCAA1452C5C26`。安装包编译日志确认 NVDA DLL 与许可证已压缩入包。
- 读取路由实测：保益会话多条 `speech_delivery` 为 `backend=保益悦听`；NVDA 会话的 F11、F12、Home、左右浏览等为 `backend=NVDA`；争渡会话为 `backend=争渡读屏`。用户已分别确认保益、NVDA 实际试玩朗读正常，并确认运行中切换读屏问题解决。
- 安装验证：1.4 覆盖安装静默退出码 0；安装后游戏、更新器文件版本 1.4.0.0，NVDA DLL 与 LGPL 许可证位于安装目录，公共桌面快捷方式指向安装目录游戏 EXE，安装前后 `settings.json` 与 `statistics.json` SHA-256 不变。按用户指示未重复卸载或全新安装。
- 服务器验证：`windows/stable/1.4`，Release ID 66，`status=published`；脚本验证旧版本可发现 1.4、1.4 显示无更新、服务器回下载文件大小与 SHA-256 和本地一致（`SERVER_VERIFICATION=OK`）。固定公钥签名密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。
- 尚未完成、不得记为通过：争渡运行中切换的逐项人工复测、Windows 讲述人完整人工验收、初级/中级/高级各 10000 局正式强度报告、公开 CA Authenticode 签名；按用户指示本轮也未重复卸载或全新安装。

## 2026-09-20 本机试用内核：首尾选牌与简洁报牌（未发布）

- 目标与边界：为单人本机试用增加“顺子两端各一张、连对两端各两张自动补齐中间点数”，并把顺子、连对、不带翅膀的飞机改成首尾范围报牌。不支持自动补飞机、三带、四带、炸弹、自动出牌或提示推荐；不改规则、计分、AI、发牌与持久化。本机试用不打包、不签名、不提交、不推送、不发布、不联网检查更新。
- 隔离方式：全部改动只发生在独立 git worktree `out/endpoint-plan-20260920-093251/work`（基线 `fe1364bc7ad5e0757d22d226f81b2350d6ef9096`），只读复用 `Qt\6.8.3\msvc2022_64`，Release 构建落在该 worktree 自己的 `build\release-x64`；原仓库在回写前保持字节不变，受版本控制文件逐项哈希核验一致。
- 产品修改文件（4 个）：`src/ui/models/hand_list_model.h`、`src/ui/models/hand_list_model.cpp`（新增 `completeEndpointSelection`）、`src/ui/main_window.cpp`（两个拿牌入口与全部放下接入）、`src/core/text/card_text_formatter.cpp`（新增首尾范围函数，飞机带对仍用旧逐点函数）。
- 测试修改文件（2 个）：`tests/ui/test_main_window_shortcuts.cpp`、`tests/core/test_card_text_formatter.cpp`。未修改 CMake，顶层 CTest 目标数仍为 19。
- 新增确定性用例 65 个（UI 目标 56、文字格式目标 9）：顺子/连对补牌的正反方向、端点区间、最小长度、缺牌失败、端点含 2 或王、单副与两副牌实体唯一性、四人模式 24 张、人数边界、放下不补回、不自动延长、单张朗读标记、拿牌顺序、信号次数、范围文字正反例、飞机带牌与翅膀文字保持、全手牌与底牌查询保持、男声/女声波形资源存在性。
- 针对性测试：`work\build\release-x64\test_card_text_formatter.exe` 20 通过、0 失败；`work\build\release-x64\test_main_window_shortcuts.exe` 108 通过、0 失败。真实输出见任务目录 `logs\02-text-tests.out.txt`、`logs\03-ui-tests.out.txt`。
- 最终全量回归：`tools/build.ps1 -Preset release-x64 -Build -Test`，CTest 19/19 通过，总耗时 83.72 秒，退出码 0；完整日志 `logs\04-final-build-test.log`。四人中级 500 局、二人/三人中级各 50 局、高级 500 局、农民协作、UI、声音和更新签名等既有回归继续通过。
- 既有用例的唯一期望更新：`testCardSelectionKeyboardCommandsAreExactAndCompatible` 中 Control+上 连续拿满 333 与 444 后，旧文案“3张4”“3张3、3张4”按本轮规格改为“3到4飞机”，因为 333444 恰好是不带翅膀的飞机，属于本轮明确约定的纯飞机文字变化。该用例的输入正是新规格覆盖的整组手势，冲突原因已写在用例内；同时新增 `testControlDownKeepsGroupTextForMixedSelection`，确认不构成牌型的混合选择（333 加 66）在 Control+下 时仍保留旧组文案“3张3、对6”。
- 自动测试的已知边界：`test_main_window_shortcuts.exe` 必须带真实窗口运行；用隐藏窗口方式启动会让窗口激活类断言失败，这是启动方式造成的，不是产品缺陷，因此该目标的真实结论以 CTest 和正常窗口运行为准。
- 本机试用内核：`work\build\release-x64\飞船单机斗地主.exe`，792,576 字节，SHA-256 `5FE69D68D1AD0FD6F2D06794F753B29AC5301172A28DEBBD4C7D62B897AF9E5A`，文件版本 1.2.0.0，不依赖 Qt6Network.dll。这是本机试用产物，不是正式安装包，也没有改变服务器上的 1.2。
- 安装替换结果：2026-09-20 09:54:40 完成安装目录单文件换入。安装版主程序 SHA-256 `5FE69D68D1AD0FD6F2D06794F753B29AC5301172A28DEBBD4C7D62B897AF9E5A`、792,576 字节、文件版本 1.2.0.0，与冻结候选逐字节一致；替换前后安装目录 194 个文件中唯一变化项是主程序，更新器与其它文件哈希未变，没有残留临时文件。替换前旧主程序已备份到任务目录 `backup\installed\old-game.exe`（哈希 `33851A1ABD5B2A9C3F403D88EFD631F196D65DD265E326B8CBF00F11960DE931`）。换入前后都确认安装版游戏与更新器进程未运行，替换后没有启动二者。用户桌面没有该游戏快捷方式，公共桌面快捷方式仍指向安装目录主程序。
- 尚未完成、不得记为通过：安装版实际启动试玩；NVDA、Windows 讲述人、争渡、保益四种读屏软件完整人工验收；初级/中级/高级各 10000 局正式强度报告；公开 CA Authenticode 签名。原始物理键盘钩子的人工实测也未做，自动化只覆盖分发入口。

## 2026-09-20 正式版 1.2

- 快捷键设置对话框已彻底删除“修改所选快捷键”按钮；自动测试确认控件不存在，功能列表按回车仍直接进入修改。
- `tools/build_release.ps1 -Package` 完成全量重新配置和编译；CTest 19/19 通过，总耗时 79.95 秒，覆盖二人/三人/四人规则与模拟、AI、农民协作、快捷键、声音和更新签名。
- 安装包 `飞船单机斗地主-Setup-1.2.exe`：61,870,243 字节，SHA-256 `AD3166EF22826493BD43361E1FFAC0C47452D5EAEF23C455DCA039FF905FBB4B`，Authenticode `NotSigned`，固定公钥发布清单签名通过。
- 1.1→1.2 覆盖安装、卸载、用户设置和统计保留、1.2 全新安装、桌面快捷方式目标及实际启动均通过。安装版游戏 SHA-256 为 `33851A1ABD5B2A9C3F403D88EFD631F196D65DD265E326B8CBF00F11960DE931`；更新器为 `A32839D45794F3330D45CFB758FF08ED1F44B92C8D0A4F1644AE1D3E1A1321A3`，二者版本均为 1.2.0.0。
- 更新服务器发布为 `windows/stable/1.2`、Release ID 64；自动验证 1.1 可发现更新、1.2 无更新，服务器下载大小、哈希、签名和密钥 ID 与本地一致。
- 尚未完成三级各 10000 局正式强度报告、四种读屏软件完整人工验收、公开 CA Authenticode 证书；不得写成已完成。

## 2026-09-20 自定义快捷键按键列表修改

- 操作流程验证：选择功能并按回车后，焦点进入可勾选按键列表；上下光标键逐项浏览，空格键选中或取消，跳格键切到“使用这个热键”，回车后才确认。
- 单键验证：从原“下翻页键”浏览到“上光标键”并按空格选择，确认后绑定准确变为无修饰键的“上光标键”。选择新的普通键时会自动取消原普通键，避免形成无法识别的双普通键组合。
- 组合键验证：用同一列表分别勾选 Control 和下翻页键，状态文本为“Control加下翻页键”；保存后单独下翻页键不触发，Control 加下翻页键在 Qt 事件与 Windows 原生消息路径均触发。
- 兼容验证：中文名称、冲突提示、JSON 保存、跳格键绑定、Alt 后菜单导航以及原快捷键的各兼容路径继续通过；Shift、Control、Alt 仍使用英文名称。
- `test_main_window_shortcuts` 全套通过；完整命令 `tools/build.ps1 -Preset release-x64 -Build -Test` 通过，CTest 19/19，总耗时 80.09 秒。
- 构建及安装内核 SHA-256 均为 `716F24C3E6C148536537CFDAC01EBE6EE42A73F649D8EFAF47BB722682B9185E`，大小 781,824 字节，文件版本 1.1.0.0。两个桌面快捷方式均指向安装目录，新快捷方式启动检查得到同一进程路径与哈希；更新器未替换。

## 2026-09-20 自定义快捷键本机试玩批次

- 设置兼容验证：旧 JSON 缺少 `shortcuts` 时保留全部默认键；自定义绑定能往返保存；损坏或重复绑定的外部 JSON 安全回退默认值。
- 名称与冲突验证：Shift、Control、Alt 按用户要求保留英文，其他按键列表、功能名、按钮和提示使用中文；“下翻页键”“回车键”等名称正确；重复绑定会提示占用该键的中文功能名且不覆盖原绑定。
- 无障碍键盘流程验证：Alt 后使用跳格键可切到菜单栏第 4 项“快捷键设置”；回车进入后可用上下光标浏览。启动回车不会被误捕获，捕获就绪时显示并发送无障碍提示“请按下你要设置的热键”；按下和松开目标键只完成选择，跳格到“使用这个热键”并回车后才确认。
- 状态隔离验证：在捕获状态按下回车键只会把“回车键”作为候选，不会同时确认；跳格键按下时不发生焦点跳转，松开后被记录为候选，下一次跳格键才切到确认按钮。冲突绑定不覆盖原值。
- 多路径验证：把 F12 对应功能改为下翻页键后，原 F12 不再触发，下翻页键在 Qt 事件和 Windows 原生键盘消息路径均能复读；把同一功能改为跳格键后，主界面 Qt 与 Windows 原生路径均按新键触发，菜单内跳格导航及原 F1/F5 兼容行为继续通过。
- 完整命令 `tools/build.ps1 -Preset release-x64 -Build -Test` 通过，CTest 19/19，总耗时 82.45 秒；原四人中级 500 局、三人中级 50 局、二人中级 50 局、高级 AI 500 局、农民协作、UI、声音和更新签名回归均继续通过。
- 构建及安装内核 SHA-256 均为 `96494488B23AC8CDA7B9D534EA22D8004DD360D75B688A411AE3F2A3BF9531D3`，大小 774,144 字节，文件版本仍为 1.1.0.0；静态依赖不含 Qt6Network。更新器未替换，且没有启动游戏或更新器进行联网相关检查。
- 尚未完成 NVDA、Windows 讲述人、争渡、保益四种读屏软件对快捷键设置对话框的人工验收；本轮自动回归不代表该人工项目已经完成。

## 2026-09-20 二人模式本机试玩批次

- 确定性发牌验证：二人局使用 ID 0 至 53 的完整单副牌，双方各 17 张、底牌 3 张且互不重复；其余恰好 17 张为盖牌，不进入两方手牌、底牌、公共快照或 AI 观察。第三、第四固定容量槽为空。
- 流程验证：叫分与出牌始终只在前两席轮转；一方过牌后上一手出牌者立即重新领出；地主获得底牌后为 20 张。二人地主/农民得失等额、第三和第四槽为零，结算零和。
- 规则与 AI 验证：二人局启用三带一、飞机带单、四带二单和四带两对，普通炸弹与王炸均翻倍；合法动作生成器覆盖同一规则。新增中级 AI 种子 1 至 50，50/50 正常结束且零非法命令。
- UI/兼容验证：设置框包含且只包含二人、三人、四人三个选项；已保存二人设置能启动二人牌局；唯一电脑位于牌桌正上方，第三、第四席牌背数均为零。非法人数仍回退到四人，旧设置默认值未变。
- 完整命令 `tools/build.ps1 -Preset release-x64 -Build -Test` 通过，CTest 19/19，总耗时 81.02 秒；原四人中级种子 1 至 500、三人种子 1 至 50、高级 AI 500 局、农民协作、UI、声音和更新签名回归均继续通过。
- 安装内核 SHA-256 为 `EE91729DCE109768BAB0C06304B5E6DE9C6C418643B6C9A6DA754D2CB35C6F37`，与构建产物一致，大小 734,720 字节，文件版本仍为 1.1.0.0；静态依赖不含 Qt6Network。更新器未替换，也未启动游戏或更新器进行联网相关检查。
- 尚未完成二人模式三级各 10000 局正式模拟及四种读屏软件人工验收；本轮结果是功能与稳定性回归，不代表这些正式发行增强项已完成。

## 2026-09-19 三人模式本机试玩批次

- 确定性发牌验证：三人局只使用 ID 0 至 53 的单副牌，三家各 17 张、底牌 3 张且 54 张无重复；第四席手牌为空，叫分期间公共底牌为空，地主确定后为 20 张并公开 3 张。
- 模式隔离验证：三人局接受三带一、飞机带单、四带二单和四带两对；同一批牌在四人规则下仍按原限制拒绝。三人王炸只翻倍，四人多级炸弹表未改。
- 流程验证：三人叫分与出牌从不进入第四席，两家连续过牌即由上一手出牌者重新领出；三人地主/农民结算零和且第四槽得分为零。
- 兼容验证：设置 JSON 往返保存三人选择，非法值及旧设置回退到四人；存档保存有效人数，旧 schema 缺少人数时恢复为四人；进行中牌局不被设置切换改变。
- UI 验证：设置框包含且只包含三人、四人两个选项；从已保存三人设置启动确实发出三人牌局；三人牌桌第四席牌背数为 0，既有快捷键、朗读、音效和无焦点鼠标穿透架构保持。
- 自动模拟：新增中级 AI 三人确定性种子 1 至 50，50/50 正常结束，零非法动作，全部结算零和；原四人中级种子 1 至 500 与高级 AI、农民协作回归继续通过。
- Release 使用 `tools/build.ps1 -Preset release-x64 -Configure -Build` 全量编译通过；最终执行 `tools/build.ps1 -Preset release-x64 -Build -Test` 为 18/18 通过，总耗时 79.72 秒。
- 安装内核替换后 SHA-256 为 `1DBB9DA8EEA75D86CA51AA6520334F80E2577EB2454F9559DB203A5C4585F615`，与构建产物一致，文件版本仍为 1.1.0.0；静态依赖只有 Qt Core/Gui/Widgets 等本地组件，不含 Qt6Network。为严格遵守本轮不连接更新服务器的边界，没有启动安装版或更新器进行联网相关检查。
- 尚未完成三人模式三级各 10000 局正式模拟、四种读屏软件人工验收和长期强度调优；本轮结果是功能与稳定性回归，不代表这些正式发行增强项已完成。

## 2026-09-11 浏览与拿放牌朗读统一

- 自动回归先取得左右浏览文本“4张7”，再验证 `Ctrl+上` 整组拿起和 `Ctrl+下` 全部放下的公告文本与其完全相等。
- 三张牌及多组断言更新为“3张3”“3张4”“3张3、3张4”；对子保持“对4”，单张保持点数。
- Release CTest 17/17 通过，总耗时 78.17 秒；其中 500 局中级模拟、500 局高级合法性、农民协作、快捷键、朗读、可视牌桌和声音回归全部通过。
- 安装版试用内核 SHA-256 为 `894C161092D15AEC7B9E1F7633D164DE3604CF85E4BDF679CB9706DACB3EDB37`，文件版本 1.0.0.0，5 秒启动检查通过后测试进程已退出。

## 2026-09-10 未发布快捷键试用内核

- 出牌阶段快捷键回归：`Ctrl+Enter` 执行过牌；单独空格不改变牌局状态；普通 `Enter` 继续出牌。
- Qt 事件与 Windows native message 两条自动回归路径均已覆盖。
- Release 增量编译成功；CTest 17/17 通过，总耗时 54.92 秒。
- 安装目录新内核 SHA-256 为 `54D8ABAA3E3B06B2DE518A4E366659C268173B787C875180747BFA35B1FF951E`，文件版本为 1.0.0.0，5 秒启动检查通过。
- 本轮仅供本机人工试玩，尚未制作安装包、升级版本或发布服务器更新。
- 第二轮试用增加拿放牌朗读：单张拿起/放下读点数，整组拿起读整组，全部放下读本次全部牌；原操作音效保留。
- 自动回归覆盖整组、连续单张、多组、单张放下和对子朗读结果，并确认可见状态栏文字与选择结果不受影响；最终整组和多组均与左右浏览统一为“张数+点数”格式。
- 第二次 Release CTest 17/17 通过，总耗时 54.48 秒；新内核 SHA-256 为 `CEAC4FFFAB63EC08009F8F0902483F3BCB9D2F8812DDA61CE65276EE35898003`。
- 退出牌局确认框自动回归：按钮文字为“是”“否”，“否”同时为 Enter 默认按钮和 Esc 按钮；按 Enter 后窗口保持打开。
- 第三次 Release CTest 17/17 通过，总耗时 54.19 秒；最终试用内核 SHA-256 为 `F51E1F7D2A716DC0F83B584874A2A78A0AB57731BDD13FEA4F516479D511FF59`，5 秒启动检查通过。
- 安装脚本当前只配置简体中文语言；未来正式打包时仍须人工复核整个安装、覆盖和卸载界面没有英文。本轮未制作安装包。
- 可视牌桌回归：组件无焦点且鼠标穿透，鼠标点击不改变手牌；键盘拿牌后可视选中数量同步。
- 隐藏信息回归：叫分阶段可视底牌数为 0；公开后显示 8 张；三家电脑仅按公共剩余张数显示牌背；上一手公开牌可显示。
- 可视渲染检查：程序绘制牌面、红黑花色、牌背、25 张真人手牌、玩家信息和当前行动者高亮均正常；调整后真人信息条无遮挡。
- 最终 Release CTest 17/17 通过，总耗时 54.71 秒；安装版试用内核 SHA-256 为 `462EC25737374939F8AE6FA1D0FF64814AC567FC9C9BC90F60FB12E4E56D26DB`，5 秒启动检查通过。
- 可视牌桌仍属未发布本机试用功能，未制作安装包或发布服务器更新。
- AI 双方案回归：拆牌束搜索能判定完整顺子型残局优于同张数零散牌；公开信息推断会使用公开过牌状态调整压制风险，相同观察与种子重复结果一致。
- 隐藏信息边界：新评估器构造函数只接收 `AiObservation` 和难度档案；抽样牌池只扣除本家手牌、已公开出牌及公开底牌，不接收 `GameState` 或 `FullGameState`。
- 农民协作 10/10 通过，包括不压队友、立即出完、地主一张守门选最低牌、压地主最低代价与相同观察决策不变。
- 最终 Release CTest 17/17 通过，总耗时 77.90 秒；两组中级模拟合计 500/500，高级 AI 自动牌局 500/500，均零非法出牌且正常结束。
- 安装版 AI 试用内核 SHA-256 为 `F08269E84B14B2D5D8E727FD3048FB15D7E4D84FE185F63A3A39C3FED92EF383`，文件版本 1.0.0.0，5 秒启动检查通过后测试进程已退出。
- 本轮未执行三级各 10000 局正式强度指标对比；当前结果属于功能与稳定性回归，AI 实战强度等待用户试玩确认。

## 确定性 AI 场景

必须覆盖：无危险时让队友单牌/对子；不用炸弹压队友；队友剩一张/两张时创造机会；地主剩一张时守门；立即出完例外；压地主使用最低代价；无力拦截时为后位保留控制牌；改变不可见暗牌后同一观察和种子决策不变。

## 自动模拟

每等级至少 10000 局，要求零非法出牌、零无法结束、零越权暗牌读取、非例外炸弹压队友为零、非紧急高牌压队友率接近零。输出地主/农民/座位胜率、让牌率、守门成功率和异常记录。

### 2026-08-17 开发期基线

- 最新 Debug 快速 CTest：14/14 通过，6.60 秒。
- 最新 Release CTest：17/17 通过，53.64 秒。
- Release `test_engine_simulation_seeds_1_250` 与 `seeds_251_500`：合计 500/500 通过。
- Debug `test_ai` 的高级 AI 自动牌局：500/500 通过，设置 900 秒函数超时后约 395.35 秒。
- `test_farmer_cooperation`：8 个确定性合作和隐藏信息不变性场景通过。
- `test_main_window_shortcuts`：40/40 通过，覆盖单独大键盘 1/2/3/4 和 Alt+数字释放行为。
- `test_sound_management`：12/12 通过。
- 当前绿色版已生成并通过 5 秒启动检查；安装包尚未生成和验收。

以上是开发期回归，不替代正式版要求的三级各 10000 局模拟与指标报告。

## 无障碍与更新器

Windows 键盘和朗读自动回归后，再以 NVDA、讲述人、争渡、保益人工验收。更新器覆盖签名、哈希、断网、取消、下载中断、替换失败回滚与隐私边界。


## 2026-08-17 Windows x64 安装版 1.0 验收

- Debug CTest：17/17 通过，总耗时 717.22 秒；两组 500 局模拟、`test_ai`、农民协作、更新签名、快捷键和音效测试全部通过。
- 最终 Release CTest：17/17 通过，总耗时 54.81 秒。
- 更新服务器测试：25/25 通过；迁移 `0005_release_signing` 已应用。
- 安装包：`飞船单机斗地主-Setup-1.0.exe`，61,834,752 字节。
- SHA-256：`ECDECD550DB5D1735B53FF4131230E28450A3451C6438B7509E427FE106305F4`。
- Authenticode：`NotSigned`；固定公钥签名验证通过，密钥 ID 为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。
- 安装、同版本覆盖安装、卸载、最终重装均成功；安装器退出码均为 0。
- Program Files 安装目录、公共桌面快捷方式、开始菜单快捷方式和卸载入口正确。
- 覆盖安装后快捷方式均保持单份，安装文件与 stage SHA-256 一致。
- 卸载删除程序目录及安装快捷方式，保留设置、统计、日志、绿色版目录和绿色版测试快捷方式。
- 最终重新安装后游戏通过 5 秒启动检查，独立更新器在当前版本检查下退出码为 0。
- 服务器发布记录：Release ID 49，`feichuan_offline_doudizhu/windows/stable/1.0/published`。
- `current_version=0.9` 能发现 1.0；`current_version=1.0` 显示无更新；服务器下载文件大小、SHA-256 和固定公钥签名均与本地一致。
- 完整四种读屏软件人工验收、三级各 10000 局正式模拟和公开 CA Authenticode 签名仍未完成，不得将这些项目记录为已通过。
