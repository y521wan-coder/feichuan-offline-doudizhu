# 开发日志

## 2026-09-25 地主首轮出牌开关与 2.2 发布候选

- 用户要求在设置中增加默认开启的地主首轮必须出牌开关，可关闭以允许地主首轮过牌；发布更新服务器与开源仓库，最后更新完整继续开发说明。单机和 AI 对战设置各自保存该值，旧配置缺字段时默认开启。
- `src/ui/main_window.cpp` 仅在真人是地主、牌局尚无任何出牌/过牌且轮到真人自由领出时禁用过牌入口；快捷键、自动过牌超时及倒计时一并受约束。关闭开关时继续使用原有领出过牌行为；后续回应阶段的过牌路径不变。AI 自由领出的合法目录本就不含过牌。
- `tests/app/test_app_settings.cpp`、`tests/ai/test_ai.cpp` 与 `tests/ui/test_main_window_shortcuts.cpp` 补充默认值、持久化、两个设置页以及首轮开启/关闭和后续过牌回归。整组主窗口测试在关闭外部读屏私有接口的环境下 117/117；正式构建 CTest 21/21。原环境下两项读屏事件计数测试仍会受本机争渡影响，真实读屏听感未作人工验收。
- 由于此前未发布的 AI 对战源码进入正式安装包，`tools/build_release.ps1` 补齐独立 AI 服务及其 Qt 网络依赖的打包步骤。2.2 安装包已构建、签署固定公钥清单并正式发布到更新服务器，Release ID 76，`SERVER_VERIFICATION=OK`。本轮未调用真实云模型、未读取 AI 对战凭据，也未重跑 90,000 局和 30,000 局长测。
- 开源仓库 `main` 已推送产品提交 `9dab809820b60090f4070140d28a3b03d7f86b5b`，注解标签 `v2.2` 解引用到该提交。安装版主程序、AI 服务、玩法说明与更新日志备份后替换，安装目录仍为 197 文件；四文件哈希及完整交接见根目录 `继续开发说明.txt`。未运行正式安装器、更新器或真实模型试玩。

## 2026-09-25 默认修改范围与 AI 策略提示词焦点修复

- 用户确定今后的默认开发原则：未明确要求其它事项时，只修改指出的功能及必要的针对性测试，完成后替换安装版内核；其它功能、设置、提示词、文档、用户数据和远端事项不顺手改动。只有用户明确要求时才做额外事项。本原则已写入 `AGENTS.md`，取代旧的每轮强制改写交接文档要求。
- 本轮按用户要求，让 AI 对战 F5 设置的“每轮 AI 策略提示词”多行编辑框使用 Tab/Shift+Tab 切换焦点，仍可正常输入文字。仅改动 `src/ui/dialogs/ai_battle_settings_dialog.cpp` 和对应的 `tests/ui/test_main_window_shortcuts.cpp` 焦点验证；没有改动默认提示词、模型请求或单机版设置。
- 增量构建通过；`test_main_window_shortcuts.exe testAiBattleF5OpensAiBattleSettings` 定向通过。主窗口全目标为 114 通过、2 失败，失败仍是此前传统焦点与讲述人公告事件计数，焦点修复用例通过，不能写作主窗口全通过。
- 已备份并替换安装版 `飞船斗地主.exe`；新文件与构建产物 SHA-256 均为 `980911A1DDBF781C82585E4DFFD233CDCFF3B05807916FBADE68BD35B939686C`，旧文件备份于 `build/release-x64/install-trial-backup-20260925-prompt-tab-focus/`。替换时游戏和 AI 服务未运行。最近提交 `14122b7`，工作区既有未提交修改保留；未提交、打包、签名、发布、推送或打标签。

## 2026-09-25 AI 对战完整牌局复制与自定义策略提示词

- 用户要求：完整保留最近 50 局 AI 对战，能一键复制供事后分析；可在 AI 对战设置里修改每轮给模型的策略文字，初始仍用原提示词，完成后替换安装版主程序与 AI 服务并重写交接说明。
- `src/persistence/ai_battle_statistics_repository.h/.cpp` 为结算牌局写入独立详细 JSON，包含公开动作牌面、顺序、动作后张数/倍数/时间、云请求耗时（毫秒与秒）、结果、动作编号、令牌数、故障码和当轮策略文字；按文件名顺序只保留最近 50 份。`src/ui/main_window.h/.cpp` 在 AI 对战游戏菜单增加复制入口，复制 50 局 JSON 数组到剪贴板；结束回合的云结果到达后重写同一文件，避免漏掉最后一次模型决策。原统计、精简回放和诊断日志格式不变。
- `src/app/ai_battle_prompt.h` 提取原策略文字为共用默认值；`src/app/ai_battle_types.*` 持久化可选的自定义文字，旧设置自动沿用默认；`src/ui/dialogs/ai_battle_settings_dialog.*` 增加多行编辑框和恢复默认按钮；`src/ai/ai_decision_request.*` 每轮携带当前文字；`apps/ai_service/ai_service.cpp` 将其用于三种协议的提示词，仍附加固定的编码说明、人数规则和合法动作输出约束。模型仍只可选合法动作编号，主引擎仍最终裁决。
- 针对性验证：增量构建通过；`test_ai_battle_persistence` 4/4，检查公开牌面、耗时、策略文字、末回合重写、敏感信息不落盘与 51→50 滚动；`test_ai_service` 4/4（本地假 HTTP，三协议）；`test_ai` 提示词设置用例 3/3；主窗口新增入口、剪贴板和提示词编辑控件相关用例 4/4。主窗口全目标仍为既有的传统焦点、讲述人公告两项失败（112 通过、2 失败），不记为全通过。未调用真实模型、未重跑完整 CTest 或长测。
- 安装版替换、文件哈希与工作区最终状态见根目录 `继续开发说明.txt`。最近提交仍为 `14122b7`；本轮未提交、打包、签名、发布、推送或打标签。

## 2026-09-25 四人规则修订与三种人数完整玩法说明

- 用户确认：四人局一小王加一大王是双王枪毙，为枪级最大；两小王加两大王是天尊无敌，为天炸级最大。牌型判定、压制顺序、音效原本正确；本轮按人数修正四人局文字、状态和朗读，保留二／三人“王炸”叫法。用户同意四人局飞机带对的对子不能与主体同点数，故牌型判定与 AI 合法动作生成同步拒绝同点翅膀。炸弹倍数与音效未改。
- 用户追加：ALT 菜单“帮助 → 玩法说明”提供二／三／四人的完整 TXT 规则。已扩写 `assets/docs/飞船斗地主玩法说明.txt`，并复制到安装版 `docs/`；现有菜单入口保持不变。`docs/规则规格.md` 同步修正四人炸弹名称。
- AI 提示词：`apps/ai_service/ai_service.cpp` 按 `player_count` 只加入本局人数的简短规则，四人分支说明双王枪毙、天尊无敌和飞机对子翅膀限制；共用胜负策略与单次请求不变。`tests/ai_service/test_ai_service.cpp` 用本地假 HTTP 检查三种协议的实际提示词，`test_ai_service` 1/1 通过、36.40 秒。真实模型棋力及延迟待用户试玩确认。
- 代码与测试文件：`src/core/text/card_text_formatter.h/.cpp`、`src/core/rules/card_pattern.h/.cpp`、`src/core/rules/pattern_analyzer.h/.cpp`、`src/core/engine/game_engine.cpp`、`src/ai/legal_move_generator.cpp`、`src/ui/main_window.cpp`；`tests/core/test_card_text_formatter.cpp`、`tests/core/test_card_pattern_sound_plan.cpp`、`tests/rules/test_pattern_analyzer.cpp`、`tests/ai/test_ai.cpp`。旧测试中使用同点翅膀的合法样例改为合法的不同点数翅膀。
- 构建和验证：`tools/build.ps1 -Preset release-x64 -Build` 成功；相关核心 CTest 5/5 通过；`test_ai.exe` 三个相关用例通过；`test_main_window_shortcuts.exe testHelpActionsOpenRealTxtFiles` 通过。主窗口全目标在结束安装版游戏后仍有 2 个读屏焦点事件测试失败（112 通过），原因未查明，本轮未将其标记为通过。未发真实模型请求，未跑完整 CTest 或长局压力。
- 安装：覆盖 `飞船斗地主.exe`、`飞船斗地主AI服务.exe` 与 `docs/飞船斗地主玩法说明.txt`，三个旧文件备份于 `build/release-x64/install-trial-backup-20260925-four-player-rules/`。新主程序 SHA-256 `E569051D5E6BB6CA7B781F7149E074498C8472DA59DC5239CD0EFE3D60FD717B`，AI 服务 SHA-256 `4EEC7BE8E0943659353A33720F400239D1A8F4757A0AF998BA2DB3FC64727D1D`，TXT SHA-256 `60336220E4DE937059CB46E1933C0365A19B43095EE1286A5263EAD0D8BC90D4`，均与来源一致。详见 `docs/2026-09-25-四人规则修订交接.md`。最近提交 `14122b7`，原工作区改动保留；未提交、打包、签名、发布、推送或打标签。

## 2026-09-24 AI 对战胜负目标提示词与单一等待上限

- 用户要求：地主争取自己先出完；农民按二／三／四人规则协作争取任意农民先出完；去掉 AI 对战的快速／均衡／深入选择，只留默认 10 秒、最高 180 秒的共用等待设置，完成后替换安装版。
- 改动：`apps/ai_service/ai_service.cpp` 明确全知输入的座位、身份、牌面、牌型编码，以及本队胜负目标、直接获胜、阻断对手、队友让牌、炸弹与叫分原则；仍只返回合法动作编号。`src/app/ai_battle_types.h/.cpp` 固定低延迟强度，默认超时 10 秒、范围 1～180 秒，旧配置默认 15 秒迁移至 10 秒；`src/ui/dialogs/ai_battle_settings_dialog.h/.cpp` 移除强度控件，改用可编辑秒数组合框（上下键选常用值或输入任意合法秒数）。故障提示不再要求切换已删除的强度档位。
- 确定性测试：`tests/ai_service/test_ai_service.cpp` 检查三种协议的实际提示词；`tests/ai/test_ai.cpp` 检查固定档位、默认值、旧配置迁移和最大值；`tests/ui/test_main_window_shortcuts.cpp` 检查新组合框和强度控件移除。增量构建成功；四个受影响 CTest 目标 4/4 通过（133.87 秒），故障提示修改后 AI 服务单独复测 1/1 通过（35.81 秒）。未发真实模型请求，棋力和真实延迟待用户试玩，未重跑未受影响的长测。
- 安装版：主程序 SHA-256 `98E1E89B7AE631C53406963B89A82F7D382DDA6930DF9C0D12FDBB17B91E661D`，AI 服务 SHA-256 `7A50DBA06C783841E7C623CADBA71215BF450FDC118A26ED29B7123BCD71452D`，均与构建版一致；安装目录 197 文件，旧文件备份在 `build/release-x64/install-trial-backup-20260924-strategy-timeout/`。替换时相关进程未运行，未读取凭据。最近提交 `14122b7`，原有工作区改动保留；未提交、打包、签名、发布、推送或打标签。

## 2026-09-24 AI 思考提示改为完全静默

- 用户要求：AI 机器人出牌前不再说“1正在思考”“2正在思考”；思考过程既不朗读也不显示，直接思考后出牌；其它行为不变。
- 改动：`src/ui/main_window.cpp` 删除云回合开始时的 `announce(...正在思考)` 播报，以及“云模型已等待N秒，最大等待M秒”计时标签的显示与每秒刷新的 `m_cloudWaitTimer`；`src/ui/main_window.h` 同步移除不再使用的 `m_cloudWaitTimer` 与 `m_cloudWaitMaximumSeconds`。请求计时仍保留 `m_cloudWaitElapsed`，逐局日志的延迟与超时统计不受影响；真人自己的“本轮操作剩余N秒”倒计时、叫分与出牌播报、故障暂停弹窗与播报、结算播报均未改动。
- 测试：增量构建成功；针对性 CTest `test_main_window_shortcuts`、`test_ai_battle_persistence` 为 2/2 通过、12.42 秒。按用户要求未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：按授权结束运行中的游戏与 AI 服务后替换主程序。新主程序 1,061,888 字节、文件版本 2.1.0.0、SHA-256 `26DCE9F34D0CE0C9989CBA1DC072772B8F852DEFD2FA991EE8406805CFD3633C`，与构建一致；上一版（`9E5A474A17C5502E8BD89B0A14FB6E651D5F5C4BBC164BAA167604E0F02F0C66`）备份在 `build/release-x64/install-trial-backup-20260924-quiet-ai/`；AI 服务未改（`49E5E4FD20A2D176777621E045830B985182B2313A8E5566460313BF9AABCDF3`），安装目录仍 197 个文件。本轮没有启动安装版冒烟、没有发真实模型请求、没有读取 `credentials.dat`。未提交、打包、签名、发布、推送或打标签。

## 2026-09-24 玩家称呼改用数字 1～4，云回合提示不再念模型名

- 用户要求：所有玩家一律用数字 1/2/3/4 代替“玩家一/玩家二/玩家三/玩家四”，出牌只播报“1”“2”；云模型思考提示不要再念模型名（设置里已经选过模型，玩家自己知道）；其它行为不变；玩家自己设过的名称仍用自己的名字。
- 称呼：`src/core/model/player.cpp` 的 `playerIdDisplayName()` 默认名称改为 `1`～`4`，`seatDisplayName()` 同步为 `1/我`、`2/下家`、`3/对家`、`4/上家`；`src/core/engine/game_state.cpp` 的初始座位名同步。单机与 AI 对战的播报、状态栏、结算与结果面板都走同一套 `playerDisplayName()`，因此统一变成数字。
- 迁移：`src/app/app_settings.cpp` 在 `normalize()` 中把旧 `settings.json` 里等于旧默认“玩家一～四”的名称视为未自定义并替换为数字；真正的自定义名称（例如“东家”“小李”）原样保留。`fromJson()` 末尾已调用 `normalize()`，所以读取设置时立即生效。
- 去掉模型名：`src/ui/main_window.cpp` 的云回合提示由“玩家二，<模型> 正在思考”改为“2正在思考”。故障暂停的弹窗与播报保持原样，仍包含认证显示名、模型与安全摘要，便于定位问题；若也要精简可再改。
- 同一称呼的其它界面文字一并统一：AI 对战设置页“1：真人（固定）”、分组“云模型（2及之后的电脑座位共用这一个）”、模式选择页说明、玩家名称菜单（“设置1号玩家名称(&1)”等）与名称对话框、音效分类说明、快捷键设置里的“查询1～4”。
- 测试：`tests/app/test_app_settings.cpp` 默认名称断言改为数字并新增 `testLegacyDefaultPlayerNamesMigrateToDigits`（旧名称迁移为数字、自定义名称保留）；`tests/ui/test_main_window_shortcuts.cpp` 的名称对话框与状态栏断言改用数字。构建成功；针对性 CTest `test_app_settings`、`test_ai_battle_persistence`、`test_main_window_shortcuts`、`test_sound_management` 为 4/4 通过、15.44 秒。按用户要求未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：按授权结束运行中的游戏与 AI 服务后替换主程序。新主程序 1,063,936 字节、文件版本 2.1.0.0、SHA-256 `9E5A474A17C5502E8BD89B0A14FB6E651D5F5C4BBC164BAA167604E0F02F0C66`，与构建一致；上一版（`79D9D20722F13B7CD962EDDA1D9226EDBB5AE221ECB66F071E781A07FC9870EF`）备份在 `build/release-x64/install-trial-backup-20260924-digits/`；AI 服务未改（`49E5E4FD20A2D176777621E045830B985182B2313A8E5566460313BF9AABCDF3`），安装目录仍 197 个文件。本轮没有启动安装版冒烟、没有发真实模型请求、没有读取 `credentials.dat`；实际听感由用户手动试玩确认。未提交、打包、签名、发布、推送或打标签。

## 2026-09-24 AI 对战电脑座位统一使用同一个云模型

- 用户试玩后决定：AI 对战里除玩家一（真人）之外，不管是二人、三人还是四人，电脑座位都用大模型；只要填入或选中一个模型，机器人就都用这一个。本轮只做这一条改动，其它行为不动。
- 代码：`src/app/ai_battle_types.cpp` 的 `AiBattleSettings::normalize()` 现在把玩家二及之后的座位统一改成云模式，取第一个同时配好认证和模型的座位作为共用配置并覆盖其余座位，因此旧的 `settings.json`（座位三是本地机器人、座位四用别的模型等）会自动迁移到同一个云模型；`validForStart()` 简化为只校验这份共用配置，`deepseek-v4-flash` 均衡／深入的拦截提示改为“电脑座位共用同一强度，请把强度改为快速”。
- 界面：`src/ui/dialogs/ai_battle_settings_dialog.h/.cpp` 删除每席的“控制方式”“本地难度”及重复的认证／模型／强度／超时控件，改为单一分组“云模型（玩家二及之后的电脑座位都使用这一个）”，保留游戏人数、玩家一固定真人、API 认证管理、测试所选模型和保存／取消；`src/ui/mode_selection_window.cpp` 的 AI 对战可访问说明同步改为“为玩家二及之后的电脑座位配置同一个云模型”。单机 2.1 的规则、AI 档位、快捷键、朗读、持久化格式与 AI 服务都没有改。
- 测试：增量构建成功；针对性 CTest `test_ai`、`test_ai_battle_persistence`、`test_main_window_shortcuts` 为 3/3 通过、96.01 秒。`test_ai` 新增 `testAllComputerSeatsShareOneCloudModel`，断言旧混合配置迁移后三个电脑座位的认证、模型、强度、超时完全一致，玩家一仍为真人且不参与云配置；主窗口 F5 用例新增“存在共用模型下拉、不存在旧的每席模型控件”断言；持久化测试继续用混合旧配置验证脱敏不落盘。按用户要求未重跑未受影响的完整 CTest、90000 局本地基线与 30000 局假模型压力。
- 安装版：先备份原主程序（SHA-256 `E2ECE1A22E584897A3DEA39298D9ED185D73D550D085A1BAFC8C374E4454535A`）到 `build/release-x64/install-trial-backup-20260924-shared-cloud/`，然后按授权结束运行中的游戏与 AI 服务并覆盖主程序。新安装版主程序 1,063,936 字节、文件版本 2.1.0.0、SHA-256 `79D9D20722F13B7CD962EDDA1D9226EDBB5AE221ECB66F071E781A07FC9870EF`，与构建输出一致；AI 服务未改，仍为 121,344 字节、SHA-256 `49E5E4FD20A2D176777621E045830B985182B2313A8E5566460313BF9AABCDF3`，与构建目录一致；安装目录仍为 197 个文件。
- 本轮没有启动安装版做冒烟、没有发真实模型请求、没有读取 `credentials.dat`；实际可玩性由用户手动试玩确认。争渡听感、完整真实对局后的回放脱敏检查、固定战略场景和 NVDA／讲述人／保益人工验收仍未通过。未提交、打包、签名、发布、推送或打标签。

## 2026-09-24 AI 对战首次超时诊断、脱敏逐局日志与 F5 修复

- 用户报告安装版 AI 对战第一次请求即超时。只读检查确认安装版主程序、AI 服务及 TLS 插件与构建产物一致；`statistics.json` 显示唯一请求在 14,996 ms 触发 `timeout`，对应玩家二 `deepseek-v4-flash`、深入档、15 秒。该配置命中交接中已知的 DeepSeek 思考模式限制，不是 AI 服务缺失或 TLS 部署错误。没有读取 `credentials.dat`，只读取了脱敏统计与设置中的模型、强度、超时字段。
- 在 `AiBattleStatisticsRepository` 增加逐局 JSONL 诊断记录：每局写入 `ai_battle/logs/ai-game-*.jsonl`，记录牌局开始、云请求开始、响应或本地拒绝结果、结算及安全的公开计数；请求发送失败、服务失败、错位响应、不存在动作和引擎拒绝也统一写入统计。日志刻意不写凭据 ID/名称、地址、提示词、原始响应、整桌手牌、底牌、盖牌、完整动作目录或牌面签名。测试新增敏感哨兵并逐项断言不落盘。
- 实机控制安装版时另发现 AI 对战按 F5 错误打开单机设置；原因是菜单动作已按模式分流，但统一快捷键分派仍固定调用 `openSettingsDialog()`。现按 `GameMode` 分流到 `openAiBattleSettingsDialog()`，新增 AI 对战 F5 回归断言。修改文件：`src/persistence/ai_battle_statistics_repository.h/.cpp`、`src/ui/main_window.h/.cpp`、`tests/persistence/test_ai_battle_persistence.cpp`、`tests/ui/test_main_window_shortcuts.cpp`。
- 为防止同一问题复发，`AiBattleSettings::validForStart` 对已经实测确认不兼容的 `deepseek-v4-flash` 均衡/深入档给出明确提示并拒绝保存/开战，要求改为快速；不静默降档、不自动重试、不影响其它模型。新增配置确定性测试。三次增量构建均成功；最终针对性 CTest `test_ai`、`test_ai_battle_persistence`、`test_main_window_shortcuts` 为 3/3 通过、96.70 秒。按用户要求未重跑未受影响的完整 CTest、90000 局本地基线或 30000 局假模型压力。
- 已备份并替换安装版主程序；当前构建版与 `C:\Program Files\飞船斗地主\飞船斗地主.exe` 均为 1,070,080 字节、文件版本 2.1.0.0、SHA-256 `E2ECE1A22E584897A3DEA39298D9ED185D73D550D085A1BAFC8C374E4454535A`。本轮 AI 服务未修改，仍为 SHA-256 `49E5E4FD20A2D176777621E045830B985182B2313A8E5566460313BF9AABCDF3`。各阶段备份位于 `build/release-x64/install-trial-backup-20260924-ai-trace*/` 与 `build/release-x64/install-trial-backup-20260924-fast-guard/`。
- 通过安装版界面把三个云座位从深入/均衡改为快速，保留 15 秒上限；F5 已实机确认打开 AI 对战设置。最小真实 UI 试玩产生 4 次云决策，全部成功并经本地引擎接受，延迟 612～1,463 ms，0 超时、0 非法、0 错位。随后确认退出，游戏与 AI 服务均已结束；新逐局日志包含 4 组开始/结束事件，禁用字段扫描全部为阴性。本轮没有完成整局，既有二/三/四人真实整局证据仍沿用；固定战略场景未运行。争渡双语音真实听感仍未收到用户确认，不能记为通过。未提交、打包、签名、发布、推送或打标签。

## 2026-09-24 新会话交接现场核对

- 按用户要求详细整理 `docs/AI对战模式交接计划.md`，并同步更新 `继续开发说明.txt`、`docs/测试与验收.md`。本轮没有修改产品源码、构建或重跑测试；沿用已通过的模式页针对性测试和完整 CTest 21/21（182.51 秒），以及此前本地 90000 局、假模型 30000 局证据。用户明确要求今后小改动只运行受影响的测试，不机械重复全套或长测。
- 只读核对：工作区仍有 15 个已修改的跟踪文件与大量新增文件、全部未提交；HEAD `14122b7`，冻结产品基线 `eafc5f07e9e373a78581ec8b061651f36f74b3e5`。安装目录 197 个文件，游戏主程序与 AI 服务 SHA-256 分别为 `BD86DA42CAFE5CD4442963D9A3EA56D824899CA21DC1D6CB2EEFE42D61EE78DE` 和 `49E5E4FD20A2D176777621E045830B985182B2313A8E5566460313BF9AABCDF3`，与当前构建输出一致；安装目录的更新器仍是 2.0.0.0。交接时用户已打开安装版，游戏与 AI 服务进程正在运行；没有结束或干预它们。
- AI 数据目录已有原加密 `credentials.dat`、新产生的 `settings.json`、`statistics.json`、`logs/` 和空的 `replays/`。本轮仅列目录、文件大小和时间，不读取密钥或统计内容；尚未收到用户对争渡双语音修复的真实听感反馈，也未完成真实对局后的统计／回放脱敏检查。下一步先接收用户试听与实际对局反馈，再按具体问题做最小修复和针对性测试。
- 已知限制仍包括 DeepSeek 思考模式的 `reasoning_only_output`；固定战略场景评分需要额外真实模型额度授权。试用内核替换及必要时关闭正在运行的游戏已获授权；版本号、打包、签名、正式发布、推送和标签未获新指令。本轮没有提交、发布或修改安装目录。

## 2026-09-24 修复模式选择页双语音

- 用户在安装版打开软件后听到两种语音同时朗读；实机检测到争渡读屏正在运行。原因是新增模式选择页的按钮获得焦点后，读屏软件会按 UIA 焦点自然朗读，代码又经 `AccessibilityService::announce` 主动调用读屏私有接口朗读同一按钮；启动默认焦点还会从构造回调额外触发一次。修复为模式页仅保留原生按钮焦点朗读及可访问名称、位置、说明，不再在焦点事件中主动播报。牌局中的 2.1 原有朗读路径未修改。
- 修改 `src/ui/mode_selection_window.h/.cpp`、`src/app/startup_controller.cpp`、`tests/ui/test_main_window_shortcuts.cpp`；新增模式页 Tab 焦点恰好一次、无额外 Announcement 事件的断言。`tools\build.ps1 -Preset release-x64 -Build` 成功；针对性 `test_main_window_shortcuts` 通过（12.31 秒）；完整串行 CTest 21/21 通过、0 失败、182.51 秒。
- 用户授权自动关闭运行中的游戏并重新替换。替换时相关程序已退出；原试用主程序备份于 `build\release-x64\install-trial-backup-20260924-voicefix\飞船斗地主.exe`，SHA-256 `3A659AD6559E9A4FA015A0DCE73EC0B7813CC115D7ECFEF36345EBFA95B80887`。安装版新主程序 SHA-256 `BD86DA42CAFE5CD4442963D9A3EA56D824899CA21DC1D6CB2EEFE42D61EE78DE`、文件版本 2.1.0.0，与构建目录一致；安装目录保持 197 个文件，AI 服务未改。
- 修复后安装版隐藏启动 3 秒正常，进入 AI 模式前没有拉起 AI 服务，测试进程已结束。已知限制：自动测试验证焦点事件和公告数量，真实语音听感仍需用户在争渡中复测；NVDA/讲述人/保益人工验收仍待完成。未提交、打包、签名、发布、推送或打标签。最近正式产品提交仍为 `eafc5f07e9e373a78581ec8b061651f36f74b3e5`，当前 HEAD `14122b7`；工作区继续保留原 15 个已修改文件及新增文件。

## 2026-09-24 AI 对战安装版内核试用替换

- 用户明确授权持续测试并替换安装版内核。先确认游戏、AI 服务、更新器和两个长测程序均未运行；`tools\build.ps1 -Preset release-x64 -Build` 成功（ninja 无需重编）。此前完整 CTest 21/21、184.43 秒，证据 `build\release-x64\ctest-full3.txt`；本轮串行复核 `test_ai_battle_persistence`、`test_ai_service`、`test_main_window_shortcuts` 为 3/3 通过、54.48 秒。单机 90000 局与假模型 30000 局结果沿用本日已完成证据，没有重复长测。
- 安装位置 `C:\Program Files\飞船斗地主`；覆盖主程序并新增 `飞船斗地主AI服务.exe`，文件数 196→197。主程序 1,049,088 字节、SHA-256 `3A659AD6559E9A4FA015A0DCE73EC0B7813CC115D7ECFEF36345EBFA95B80887`、文件版本 2.1.0.0；AI 服务 121,344 字节、SHA-256 `49E5E4FD20A2D176777621E045830B985182B2313A8E5566460313BF9AABCDF3`，均与构建目录相同。现有 `Qt6Network.dll` 与 `tls\qschannelbackend.dll` 哈希与构建目录相同且安装前后不变；主程序二进制中不含 `Qt6Network.dll` 导入字符串。
- 旧试用内核备份及前后清单在 `build\release-x64\install-trial-backup-20260924\`；旧主程序 SHA-256 为 `59628C68AE8C7F804D3ADB35FE4F786E7FCA96A620BF9316BE04B130D3277C10`。安装后短暂隐藏启动主程序 4 秒：主程序持续运行、进入 AI 对战前 AI 服务未启动，测试进程已结束；未运行更新器。启动会创建空的 AI `logs/`、`replays/` 目录；现有 DPAPI 加密 `credentials.dat` 未读取或改写，仍没有实际对局产生的 `statistics.json` 或回放文件。
- 本轮修改文件：`docs/开发日志.md`、`docs/测试与验收.md`、`docs/AI对战模式交接计划.md`、`继续开发说明.txt`；产品源码未改。已知限制：DeepSeek 思考模式仍会出现 `reasoning_only_output`；真实模型固定战略场景评分、安装版实际试玩、四读屏人工验收、认证管理和真实故障走查、真实 UI 对局后的脱敏文件检查仍待完成。最近正式产品提交 `eafc5f07e9e373a78581ec8b061651f36f74b3e5`，当前 HEAD `14122b7`；工作区保留 15 个原有已修改文件及大量新增文件，全部未提交。未打包、签名、发布、推送或打标签。

## 2026-09-24 正式发布 2.1（最低压制与快捷键精简）

- 用户实际试玩本机试用内核后确认效果完美，指定正式版本为 2.1，并要求发布更新服务器和开源仓库；短期内不再继续调整。产品改动只包含取消 Shift+左/右浏览牌，以及机器人回应对手时优先使用刚好能压住的最低牌；立即出完获胜、农民团队协作、规则、读屏、音效、资源、持久化和更新器逻辑不变。
- 版本与构建：`version.txt` 升至 2.1，内置更新日志增加两项说明。`tools\build_release.ps1 -Package` 完成全量重新配置、109 个编译步骤、CTest 19/19（143.60 秒）和 Inno Setup 6.7.3 编译（23.578 秒）。主程序静态依赖不含 Qt6Network。
- 二进制：`releases\飞船斗地主-Setup-2.1.exe` 为 61,996,767 字节、SHA-256 `72D21F9BE12F0991F79F2C3E27B5FE4EDE0C273EAB3F7AA3005CBEF948B2CD2A`、VersionInfo 2.1、Authenticode `NotSigned`。主程序 856,576 字节、SHA-256 `7D0DE3129261B89D885C1AA63702375EABA295BDFE3D95963F623F9DB3EC3524`；更新器 72,192 字节、SHA-256 `1532EE17B58C1C0DB75C755F368A813A753874C925600FB190F815D09FDE4B7F`；二者文件版本均为 2.1.0.0。
- 签名与服务器：`python tools\sign_release.py` 使用 Windows DPAPI 私钥生成固定公钥清单，密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。`python tools\publish_update.py --ssh-key <仓库外私钥>` 发布 `windows/stable/2.1` 成功，Release ID 75、`status=published`；0.9 可发现 2.1、2.1 显示无更新，API 大小/哈希/签名字段与本地清单一致，服务器回下载文件逐字节一致（`SERVER_VERIFICATION=OK`）。
- 本机安装目录仍是用户已经试玩确认的功能内核，文件版本 2.0.0.0、SHA-256 `59628C68AE8C7F804D3ADB35FE4F786E7FCA96A620BF9316BE04B130D3277C10`；本轮没有运行正式 2.1 安装包，没有启动游戏或更新器，也没有改变安装目录。正式 2.1 安装/卸载循环、四读屏完整人工验收、物理键盘钩子人工实测及公开 CA Authenticode 签名仍未执行，不得记为通过。

## 2026-09-24 本机试用内核：取消 Shift+左右浏览并固定最低压制

- 快捷键：删除“Shift 浏览上/下一个点数组”两个动作和默认绑定；Qt 键盘事件、Windows 原生消息与低级钩子消息均把单独的 Shift+左/右作为无操作处理。旧设置中的 `previousCard`、`nextCard` 字段自然忽略，快捷键设置也拒绝重新绑定单独的 Shift+左/右；普通左/右、Home/End、拿放牌、朗读和其它自定义快捷键不变。
- AI：机器人回应对手时，在合法动作和既有农民团队规则过滤之后，先保留能立即出完获胜的动作；否则硬性保留刚好能压住的最低档动作，顺序为非炸弹优先、最小主体点数、最小带牌点数。领出、队友让牌、地主一张必须拦截、立即获胜、牌型比较和全知信息边界不变。
- 测试：新增地主三档均以 4 压单 3、农民以三张 4 带最小对子压三张 3 带对、地主可炸完时仍立即获胜的确定性 `AiObservation` 用例；快捷键覆盖 Qt 和 Windows 钩子消息无操作、普通左右仍工作及旧动作不再进入设置。最终 `tools\build.ps1 -Preset release-x64 -Build -Test` 为 CTest 19/19 通过，0 失败，总耗时 142.24 秒；快捷键目标 110/110。
- 本机只替换 `C:\Program Files\飞船斗地主\飞船斗地主.exe`，未运行安装版或更新器。替换前后安装目录均为 196 个文件，逐文件 SHA-256 比较唯一变化是主程序；新主程序 856,576 字节、文件版本 2.0.0.0、SHA-256 `59628C68AE8C7F804D3ADB35FE4F786E7FCA96A620BF9316BE04B130D3277C10`。更新器哈希仍为 `9A9DCFE8A6AB1A4362A1CE960C43A05B9EBBE8DF6C2C5814266A7E331D73EEE8`。
- 本轮未改版本号、安装包、更新器、发布清单、服务器或远端仓库，未签名、未联网、未发布。工作区保留此前 2.1 回退文档的未提交改动，并叠加本轮源码、测试和交接记录；没有丢弃或覆盖既有修改。

## 2026-09-24 撤回 2.1 并完整恢复正式版 2.0

- 用户实战认为 2.1 的 AI 水平存在较多问题，明确要求彻底撤回 2.1，不再继续修补该版本。仓库 `main` 从 `591a4b4` 强制恢复到 2.0 发布后的 `2375ca0`，本地和远端 `v2.1` 标签均删除；`v2.0` 保留并仍指向正式 2.0 代码提交 `05a3903`。本轮产品代码未改，只补记回退状态。
- 服务器删除原 2.1 Release ID 73 及其包文件；随后使用仓库既有正式 2.0 安装包和固定公钥清单重新发布。当前唯一 stable 记录为 2.0、Release ID 74、`status=published`，服务器回下载大小、SHA-256 和签名验证通过（`SERVER_VERIFICATION=OK`）。0.9 可发现 2.0，2.0 和 2.1 均显示无更新；旧 2.1 包存储路径确认不存在。
- 本机静默卸载 2.1，退出码 0；按用户明确要求永久删除旧安装目录、注册表残留、快捷方式和 `C:\Users\apple007\AppData\Roaming\Feichuan\feichuan_offline_doudizhu` 全部用户数据，未创建 2.1 备份。随后从 `D:\飞船斗地主-2.0回退\飞船斗地主-Setup-2.0.exe` 全新安装，退出码 0。
- 当前安装目录为 `C:\Program Files\飞船斗地主`，共 196 个文件；注册表版本为 2.0，公共桌面快捷方式目标正确。主程序 855,040 字节、SHA-256 `38CB6771F787FFAA57CB57D0BF7A0730ABDBE18739D58D6E3F0016440ADBECBD`；更新器 72,192 字节、SHA-256 `9A9DCFE8A6AB1A4362A1CE960C43A05B9EBBE8DF6C2C5814266A7E331D73EEE8`；二者文件版本均为 2.0.0.0。
- 安装后未启动游戏或更新器，用户数据目录尚未重新创建。撤回前用户报告的保益语速、保益无声和 Shift+左右浏览问题本轮未修改、未验证为已解决；需在 2.0 实际试玩后按反馈另行处理。四读屏完整人工验收、物理键盘钩子人工实测和公开 CA Authenticode 签名仍未完成。

## 2026-09-23 正式发布 2.0（二人、三人牌型音效）

- 用户确认本机试用内核的出牌音效已正常，明确要求正式版本为 2.0，并更新服务器、开源仓库及交接说明。仅修复此前确认的四种单副牌牌型音效；未修改规则、AI、键盘、朗读、资源文件或更新器逻辑。
- 版本与内置更新日志：`version.txt` 为 2.0，`assets/docs/飞船斗地主更新日志.txt` 增加修复说明。`.\tools\build_release.ps1 -Package` 完成全量 Release 构建、CTest 19/19（162.56 秒）和 Inno Setup 编译。主程序依赖表未见 Qt6Network。
- 安装包 `releases/飞船斗地主-Setup-2.0.exe` 为 61,995,229 字节，SHA-256 `2A4A4A65BA7E5E9290D9489C9AAC9040699DCA44BC5DFA80C5454B09F54BB83B`，VersionInfo 2.0.0，Authenticode `NotSigned`。主程序和更新器文件版本均为 2.0.0.0。
- `python tools/sign_release.py` 使用原固定公钥配套的 DPAPI 私钥签名；`python tools/publish_update.py --ssh-key <仓库外私钥>` 发布到 `windows/stable/2.0`，Release ID 72、`status=published`。回下载大小与 SHA-256 与本地完全一致（`SERVER_VERIFICATION=OK`）；另用仓库固定公钥独立复验 1.9 更新检查返回签名成功，1.9 可发现 2.0，2.0 无更新。
- 文档更新：`docs/开发日志.md`、`docs/测试与验收.md`、`docs/更新协议.md`、根目录 `继续开发说明.txt`。本机未安装或启动正式 2.0；四读屏人工验收、安装/卸载验收和公开 CA Authenticode 签名仍未完成。正式版源码提交 `05a3903`（`fix: release 2.0 card pattern sounds`）已推送 `origin/main`，注解标签 `v2.0` 已推送；本次发布结果由随后的 `docs: record 2.0 publication` 文档提交补记。该文档提交完成后工作区应干净。下次版本为 2.1。

## 版本编号约定（2026-09-23）

- 用户确定本次音效修复正式版为 2.0；今后同一大版本按 2.1、2.2、……、2.9 递增，之后进入 3.0，并按相同规则继续。不得把 2.9 后的下一版写成 2.10；每次发布仍须高于服务器现有版本，且不得重复发布同一版本。

## 2026-09-23 二人、三人牌型出牌音效修复（本机试用内核）

- 原因：`CardPatternSoundPlan` 对单副牌允许的三带一、飞机带单、四带二单、四带两对直接返回空音效计划；出牌流程因而无声音。只补齐这四种牌型的现有 WAV 调用，不改规则、AI、界面或音效资源。
- 源码修改：`src/core/audio/card_pattern_sound_plan.cpp`；回归用例：`tests/core/test_card_pattern_sound_plan.cpp`，验证二人、三人共用的单副牌牌型、男女声文件存在及飞机带单的具体序列。
- 当前 1.9 源码增量构建成功，`tools/build.ps1 -Preset release-x64 -Test` 为 19/19 通过。为保留旧安装目录的程序名、更新器路径和文件版本，另以 `12b7be8` 的 1.8 代码与同一修复在隔离目录构建 1.7.0.0 试用内核；构建成功，音效、牌局、AI 等 18/19 个 CTest 目标通过。唯一失败的 `test_main_window_shortcuts` 为两项读屏事件捕获断言；同机对 1.9 测试程序单独重跑读屏断言也发生失败，未认定为本次音效回归通过。没有启动安装版或执行人工读屏验收。
- 只替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`。安装目录替换前后均为 196 个文件，逐文件 SHA-256 比对唯一变化是该 EXE：`DFA19DB66E53F4429B3AABC9F0B297FA7CD573007A843D98216ECD1F723F4D42` → `150CDBF9D9E4DC9FAA5431C481F90EB23005CD0642D1C26C3515E3E67F17DBC5`；文件版本仍为 1.7.0.0。替换前内核备份在 `artifacts/kernel-replacement-20260923/backup/installed-game-before-audio-fix.exe`。安装包、更新器和服务器未改。
- 试用阶段最近提交仍为 `d5b7f1e`，源码与测试修改暂留工作区；用户随后亲自实测音效正常，并明确指定正式版本为 2.0，发布结果见上文。

## 2026-09-23 正式发布 1.9（整体迁移、改名与发布）

- 用户要求把整个项目文件夹从 D 盘迁到 C 盘根目录、改成英文名称、把软件名称改成“飞船斗地主”，并同步更新服务器、开源仓库与继续开发说明。开发目录最终定为 `C:\Feichuan-Doudizhu`。
- 迁移：`D:\飞船单机斗地主` 已不存在，完整项目（含 .git、构建产物、Qt、发布包与历史验收证据）位于 `C:\Feichuan-Doudizhu`，Git 工作区与提交历史完整。
- 改名采用“外部名称更新、内部身份保持”：界面标题、EXE 与更新器文件名、安装包名、快捷方式、说明文档全部改为“飞船斗地主”；`feichuan_offline_doudizhu`、安装器 AppId `{6D733A47-91F6-4B19-A70D-48F9D8231C81}`、应用数据标识与固定发布公钥不变；安装脚本新增对旧名称快捷方式与旧文件的清理。
- 工具链：迁移后项目内 Qt 目录为空，按 `aqtinstall.log` 中的原命令恢复 Qt 6.8.3 msvc2022_64；英文路径使 `tools\build.ps1` 不再触发 SUBST 映射盘分支。
- 构建与打包：`tools\build_release.ps1 -Package` 全量 Configure + Release 构建 + CTest 19/19 通过（160.90 秒），Inno Setup 成功（23.391 秒），生成 `releases\飞船斗地主-Setup-1.9.exe`（61,994,470 字节，SHA-256 `98CD78549A78548B0AA1577EB213EAED9E8F17B9E2FF1B5A1C06694169B3FC42`，VersionInfo 1.9.0）。
- 服务器端：首次发布被服务端固定产品名校验拒绝（“产品名称或 product_key 不符合飞船单机斗地主固定配置”），当时未产生任何发布记录。随后在备份服务器源码与数据库到 `/opt/update-server/backups/pre-rename-20260923-012545/` 后，把命令固定产品名与数据库 `Product` 名称同步改为“飞船斗地主”，重建 `deploy-web` 镜像，服务器自测 33/33 通过；客户端只校验 `product_key` 与固定公钥签名，旧版升级路径不受影响。
- 发布：`python tools\sign_release.py` 生成固定公钥清单；`python tools\publish_update.py --ssh-key C:\apple1007` 发布 `windows/stable/1.9` 成功，Release ID 71、`status=published`。0.9/1.7/1.8 可发现 1.9，1.9 无更新，服务器回下载大小、SHA-256 与本地一致，并用仓库固定公钥按更新器原文格式独立复验签名通过（`SERVER_VERIFICATION=OK`）。发布后服务器只保留 1.9 记录。
- 文档：更新 `AGENTS.md`、`README.md`、`docs\更新协议.md`、`docs\测试与验收.md`、`docs\开发日志.md` 与根目录 `继续开发说明.txt`（新增新电脑灾备恢复一节）。
- 灾备：按用户要求在 `D:\飞船斗地主发布密钥灾备` 建立离线恢复包（加密私钥、十六进制密码文件、固定公钥、密钥 ID、`recover-release-key.py` 与恢复说明），并验证十六进制密码可解密该私钥、公钥与仓库登记的密钥 ID 一致；原私钥密码是 32 字节随机数据而非文本，故以十六进制形式保存。
- 未完成：1.9 安装包的覆盖/全新安装、启动、快捷方式、卸载与用户数据保留验收，用户实际试玩，四读屏完整人工验收与物理键盘钩子人工实测。

## 2026-09-23 正式发布 1.8（Shift 浏览、叫分收尾与地主极简提示）

- 用户实测发现 Shift+左/右沿同点数的每张实体牌停留，而朗读文本又是整组张数，因此“3张3”会重复三次、“4张4”会重复四次。将 `PreviousCard`/`NextCard` 的 Qt、Windows native message 和低级键盘钩子兼容路径统一为按未拿起牌的点数组跳转；同一点数现在只停留、朗读一次，普通左/右、拿放牌、牌型与已拿起牌过滤不变。
- 仅在真人叫到地主时，删除公开底牌公告之后延迟出现的“叫分结束，开始出牌”；“地主已经确定。公开底牌…地主先出牌”保留。电脑成为地主时的原有公告与 AI 等待时序未改。
- 地主确定后，大键盘 1/2/3/4 查询到地主时由“玩家名，剩余 X 张牌，地主”精简为“地主，还剩 X 张牌”；查询农民仍保留玩家名、剩余张数和农民身份。地主过牌前的名称公告也改为“地主”，原过牌音效及 300 毫秒后播放时序不变；农民过牌仍使用原玩家名。
- 自动验证：针对性覆盖 Qt 键盘、Windows 钩子消息、三张/四张同点数跳组、真人叫到地主后等待 6.2 秒不再出现冗余公告、数字键查询地主/农民、地主过牌公告及原生音效路径。`test_main_window_shortcuts.exe` 最终 110/110 通过（12.774 秒）；`.\tools\build.ps1 -Preset release-x64 -Build -Test` 为 CTest 19/19 通过，总耗时 160.33 秒。
- 试用阶段用户明确授权后强制结束了正在运行的安装版游戏进程；随后只替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`。安装目录前后均为 196 个文件，逐文件 SHA-256 比对的唯一变化项是主程序；试用内核为 852,992 字节、文件版本 1.7.0.0、SHA-256 `DFA19DB66E53F4429B3AABC9F0B297FA7CD573007A843D98216ECD1F723F4D42`，更新器仍为 `C0785E7DB8310356917BF84A1881F8AC11F6B49D467211491228815022A101DF`。旧内核备份为 `artifacts\kernel-replacement-20260923\backup\installed-game-before.exe`。
- 用户随后明确要求发布。`version.txt` 升至 1.8；`tools\build_release.ps1 -Package` 完成全量 Configure、Release 构建和 CTest 19/19（159.92 秒），Inno Setup 编译成功（23.875 秒）。正式安装包 `releases\飞船单机斗地主-Setup-1.8.exe` 为 61,994,151 字节、SHA-256 `0E565F269418ADC1EC4497C4AB731EC230BCDC8510117E251FF0D831D830C355`、VersionInfo 1.8.0、Authenticode `NotSigned`。
- 正式打包的主程序为 852,992 字节、SHA-256 `8B2570B66B8AFABDB73045E8C427353F24D5FB631D695F03B7AD42A790332D1D`；更新器为 72,192 字节、SHA-256 `C1105769593F0F92F5319424870B6DB31620C14AF0C8FA8CA0012501F66F38C9`，文件版本均为 1.8.0.0。依赖表确认主程序不含 Qt6Network，更新器保留 Qt6Network。
- `python tools\sign_release.py` 使用 Windows DPAPI 生成固定公钥清单，密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`；`python tools\publish_update.py --ssh-key <仓库外私钥>` 发布 `windows/stable/1.8` 成功，Release ID 70、`status=published`。脚本确认旧版可发现 1.8、1.8 无更新，服务器回下载大小、SHA-256 和签名均与本地一致（`SERVER_VERIFICATION=OK`）。
- 本轮未用正式 1.8 安装包覆盖或全新安装；本机仍是 1.7 安装内容加上述试用主程序。正式 1.8 的安装、启动、快捷方式、卸载和用户数据保留验收，以及 NVDA/讲述人/争渡/保益四读屏完整人工验收和物理键盘钩子人工实测均未执行，不得记为通过。
- 开源仓库提交 1.8 源码、测试、文档与版本文件，并创建 `v1.8` 标签推送到 `origin/main`。

## 2026-09-22 正式发布 1.7（AI 全知整桌推演）

- 用户要求把机器人做成“全知”强度：AI 决策可以看到真实整桌牌。产品侧只改 AI，规则、界面、键盘、朗读、声音、设置、持久化、无障碍和更新器均未改动。
- `AiObservation` 新增 `AiFullInformation`（真实整桌手牌、真实底牌、二人模式在局外的 17 张盖牌）。`makeAiObservation` 在注入前逐张校验牌 ID 合法、不重复、不超过当前人数牌堆容量，二人模式另校验盖牌恰好 17 张；任一校验失败即 `available=false`，全知推演整体退出。AI 仍不持有 `GameState`、`FullGameState`、洗牌种子或未来牌序，全知内容也不写入公共快照、可视牌桌、回放、统计和诊断日志。
- 新增 `src/ai/full_information_search.h/.cpp`：在复制牌桌上执行带硬截止时间和节点预算的同队极大/敌队极小搜索加有限轮次推演，总剩余牌不超过 `activePlayerCount * 6 + 2` 时自动切换为更深的残局搜索；候选按难度裁剪，推演内部继续套用农民强制协作规则，因此不会出现炸弹压队友或放弃立即出完。`StandardAiPlayer` 新增 `AiInformationMode`，默认 `FullInformation`；`ImperfectInformationSearch` 保留为 `PublicInference` 显式测试模式。
- `BiddingStrategy::decideBid` 新增可选全知参数：有全知信息时改用“本家手牌加真实底牌”的牌力减去最强真实对手牌力，替代旧的假设底牌抽样。`FarmerTeamStrategy` 在队友只剩一张且已知其确切点数时，用真实可被压过的上限选喂牌，原因码 `feed_safe_single_to_one_card_teammate`；未知点数时仍用原来的低单牌规则。
- 难度档案新增 `fullInformationRootCandidates`、`fullInformationNodeBudget`、`rolloutPlies`：初级 2 / 120 / 4，中级 3 / 500 / 8，高级 4 / 7000 / 20；决策预算、搜索深度、候选裁剪和安全随机度沿用 1.6。
- 测试边界随产品边界同步改写：原“不可见暗牌不能改变决策”类断言改为“相同全知输入产生相同决策”，并新增全知快照不重不漏、真实底牌可改变高级叫分、真实对手手牌可改变出牌决策、三级决策硬截止时间不超预算且回退仍然合法等确定性用例；农民协作新增“已知队友只剩可被压过的单张时安全喂牌”。CTest 目标仍为 19，没有删除测试目标或降低门槛。
- 正式模拟：`ai_validation_runner.exe stability` 覆盖二人/三人/四人 × 初级/中级/高级共 9 组，每组 10000 局，合计 90,000 局全部正常结束，`illegal`、`unfinished`、`duplicate_deals`、`set_aside_played`、`teammate_bombs` 全为 0；最慢单次决策 215.171 毫秒（四人高级），仍在 1000 毫秒预算内。结果保存在 `artifacts/ai-validation-1.7/`。本轮未重跑 1.6 对 1.7 的同种子强度对照，不得声称 1.7 比 1.6 更强。计数于 2026-09-24 由“12 组/120,000 局”订正为“9 组/90,000 局”，因原表述与产物不符（该目录只有 9 组结果文件加 9 个空错误文件）。
- 构建与打包：`tools\build.ps1 -Preset release-x64 -Configure -Build -Test` 通过，CTest 19/19，总耗时 154.69 秒；`tools\build_release.ps1 -Package` 再次全量 Configure、Release 构建和 CTest 19/19 通过，总耗时 154.48 秒，生成 `releases\飞船单机斗地主-Setup-1.7.exe`。Inno Setup 只有既有的 `PrivilegesRequired=admin` 与 `userdesktop` 混用提示，编译成功。
- 版本与二进制：`version.txt` 升为 1.7，游戏与更新器文件版本均为 1.7.0.0。安装包 61,994,721 字节、SHA-256 `FA2B542FF68CF7C47697D5A7543C8CA2AF7A1051BAAAC1CD258BA2EFCD134A86`、VersionInfo 1.7.0、Authenticode `NotSigned`；打包构建的游戏主程序 852,992 字节、SHA-256 `4392CEC79CC0F68192BF1F1C2230EE5FB1FAFFBC66D8D19BD867A5DC555CF659`，更新器 72,192 字节、SHA-256 `C0785E7DB8310356917BF84A1881F8AC11F6B49D467211491228815022A101DF`。依赖表确认主程序不含 Qt6Network，只有更新器联网。MSVC 链接写入时间戳，同一源码两次构建大小相同但哈希不同，PE 哈希只用于比对同一次构建。
- 签名、安装与发布：`python tools\sign_release.py` 使用 Windows DPAPI 生成 `releases\飞船单机斗地主-Setup-1.7.release-manifest.json`，密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`；静默覆盖安装本机 1.4 退出码 0，安装后版本、哈希、NVDA DLL、LGPL 许可证、快捷方式和用户数据全部符合预期，隐藏窗口 5 秒启动检查通过；`python tools\publish_update.py --ssh-key <仓库外私钥>` 发布 `windows/stable/1.7` 成功，Release ID 69、`status=published`，服务器回下载大小、SHA-256 与本地一致（`SERVER_VERIFICATION=OK`）。
- 文档：更新 `AGENTS.md`（AI 可见信息边界改为全知模式，其余不可变边界保留）、`docs\架构说明.md`、`docs\AI协作规则.md`（补充全知推演边界与难度参数表）、`docs\测试与验收.md`、`docs\更新协议.md` 和根目录 `继续开发说明.txt`，并补充 1.7 的全知 AI 验证与发布记录。
- 仓库更新：1.7 源码、测试、文档与版本文件提交，创建标签 `v1.7` 推送到 `origin/main`。

## 2026-09-21 正式发布 1.6（AI 整桌推演与残局搜索）

- 用户明确要求在不增加其它功能、不影响其它模块的前提下只提高机器人水平。产品修改限定在 AI 决策：新增 `src/ai/imperfect_information_search.h/.cpp`，并由 `StandardAiPlayer` 在既有合法动作、农民团队安全过滤和本家拆牌评估之后调用。用户使用桌面 1.6 测试内核实战后明确确认效果完美，并指示发布服务器和开源仓库。
- 新搜索仅从 `AiObservation` 的公开信息生成确定性可能牌局；按公开剩余张数分配未知牌，公开底牌仍按已知归属固定，二人未参与盖牌保留为未分配余量。对少量快速评估最佳候选模拟整桌轮转、接牌、过牌、牌权重置和农民团队目标；总剩余牌较少时改用有节点上限的同队极大/敌队极小残局搜索，非残局使用确定性有限轮次推演。多样本以平均结果和最坏结果组合，真实暗牌不进入决策或日志。
- 农民硬规则仍先于搜索执行；新增的战略过牌只在上一手为地主、后位公开为一至两张农民、地主多于一张且公开样本搜索显示明显更安全或队友可强制出完时启用，原因码为 `strategic_pass_for_teammate_finish`。初级不进入新搜索，中级只在更窄残局窗口使用 3 个样本，高级战术窗口使用 8 个样本和最多 4 个根候选；曾试验 12 样本/6 候选，四人胜率无增益且最大耗时升至约 156.654 毫秒，故恢复更高效配置。
- 新增非默认 `test_ai_strength` 配对强度工具。修改前旧高级对中级原始结果为三人 101/200、四人 105/200；修改后同种子、同中级叫分、全中级角色基准与仅替换一个团队为高级的结果为：二人地主 58→78、农民 42→56；三人地主 44→51、农民 56→70；四人地主 14→22、农民 86→90（每个角色各 100 局）。最大高级单次决策约 135.789 毫秒。
- 确定性测试新增后位农民一张可走时的战略过牌场景；`test_farmer_cooperation` 11/11 通过。首次完整 `tools\build.ps1 -Preset release-x64 -Build -Test` 为 19/19，通过，总耗时 218.53 秒；随后将 `version.txt` 从 1.5 升为 1.6，执行 `tools\build_release.ps1 -Package`，再次全量 Configure、Release 构建和 CTest 19/19 通过，总耗时 220.15 秒。
- 1.6 候选安装包 `releases\飞船单机斗地主-Setup-1.6.exe` 为 61,991,701 字节、SHA-256 `82D2D23189BA73E5400FC5B854B2C0A168593117682861949A87D319203CBBFF`、VersionInfo 1.6.0、Authenticode `NotSigned`；相同哈希副本已放到 `D:\飞船单机斗地主-Setup-1.6.exe`。游戏主程序 842,240 字节、SHA-256 `B529EE3BDC4E59794468BA6B0E478CC96AD96CA8E052224BAE7A5B08FBB8DF09`；更新器 72,192 字节、SHA-256 `9923F68FE9AA61E8DAAC33AC633B88920911214FD9B12141197DF67DB4A99011`，两者文件版本 1.6.0.0。安装暂存仍含 NVDA Controller Client 与 LGPL 许可证；静态依赖确认游戏主程序不含 Qt6Network，只有更新器引用。
- 创建个人桌面 `飞船单机斗地主-AI测试版.lnk`，目标为 `build\release-x64\飞船单机斗地主.exe`，不覆盖公共桌面正式版快捷方式。用户已实战确认该内核；未执行 1.6 覆盖/全新安装、用户数据保留、卸载和四读屏联合验收。
- `python tools\sign_release.py` 使用 Windows DPAPI 成功生成固定公钥清单，密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。`python tools\publish_update.py --ssh-key <仓库外私钥>` 发布 `windows/stable/1.6` 成功，Release ID 68、`status=published`；旧版发现新版、1.6 无更新及服务器回下载大小、哈希和签名全部一致（`SERVER_VERIFICATION=OK`）。按用户指示，发布阶段未重复构建或测试。

## 2026-09-21 正式发布 1.5（Windows 讲述人 UIA 公告支持）

- 本轮产品范围严格限定为 Windows 讲述人的动态朗读支持；没有修改规则、AI、牌局、键盘、界面、声音、设置或更新器逻辑。`ScreenReaderBridge` 新增 `Narrator.exe` 会话识别和 `StandardAnnouncement` 投递结果；NVDA、保益、争渡的既有私有接口仍优先，只有没有其它可用私有接口且讲述人运行时才选择讲述人标准路径，避免 UIA 广播导致两个读屏同时朗读。没有讲述人时继续使用原 `QAccessible::Focus` 兜底。
- `AccessibilityService` 对讲述人发送 Qt 6.8 `QAccessibleAnnouncementEvent`：普通公告使用 `Polite`，高/紧急优先级使用 `Assertive`；公告绑定既有状态对象，不移动真实键盘焦点。没有引入 SAPI 5、第三方组件或新的网络依赖。用户已使用专门桌面测试快捷方式实际确认讲述人朗读正常。
- 新增自动回归 `testNarratorUsesStandardAnnouncementWithoutMovingKeyboardFocus`，验证讲述人路径只产生 Announcement、不产生伪焦点、消息和 Assertive 优先级正确、键盘焦点保持、后端诊断名称正确；原传统焦点兜底用例继续通过。测试强制开关只在 `FPDZ_DISABLE_SCREEN_READER_APIS=1` 时有效，不影响产品运行路由。
- 正式打包第一次全量 CTest 为 18/19，`test_main_window_shortcuts` 在整套顺序中无断言文本失败；该 UI 目标随后连续复跑 3/3 通过。重新执行 `tools\build_release.ps1 -Package` 后全量 Configure、Release 构建及 CTest 19/19 通过，总测试耗时 82.69 秒，安装包成功生成。
- 正式安装包 `releases\飞船单机斗地主-Setup-1.5.exe` 为 61,978,972 字节，SHA-256 `842A46D680594297D3763ADCA369483AA9B6E0E3263A19CEF0865C306EE3E0D7`，Authenticode `NotSigned`，VersionInfo 1.5.0。构建目录游戏主程序 807,936 字节、SHA-256 `44973569653425077592D92A067991A645CD572C310B30033CF00769A25DC521`；更新器 72,192 字节、SHA-256 `1D32454BC31110D269F3F3734EF8E4FD3D4F484C19E1001635896A30B73C5DB4`，两者文件版本均为 1.5.0.0。主程序依赖表不含 Qt6Network；打包日志确认 NVDA DLL 与许可证仍在安装包内。
- `python tools\sign_release.py` 使用 Windows DPAPI 成功签名；固定公钥密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`，清单为 `releases\飞船单机斗地主-Setup-1.5.release-manifest.json`。
- `python tools\publish_update.py --ssh-key <仓库外服务器私钥>` 发布成功：`windows/stable/1.5`，Release ID 67，`status=published`；脚本确认旧版本可发现 1.5、1.5 显示无更新，服务器回下载文件大小、SHA-256 和签名与本地一致（`SERVER_VERIFICATION=OK`）。
- 按用户明确指示，本轮不再重复讲述人测试，也未执行覆盖/全新安装、启动、快捷方式、卸载和用户数据保留测试；安装目录仍为正式版 1.4。本轮发布结论来自用户对 1.4.0.0 测试内核的讲述人实测、自动回归和正式打包核验，不得把未执行的安装验收写成通过。

## 2026-09-20 正式发布 1.4（读屏官方接口与热切换修复）

- 用户实测现象为争渡能够朗读，而直接以保益或 NVDA 启动游戏时动态公告无声；随后又发现游戏运行中切换读屏（例如保益切到争渡）后无声，只有重启游戏才恢复。排查确认近期实现只有 Qt/UIA 传统焦点事件，没有调用任何读屏官方接口；切换后 Qt 标准后端仍绑定旧读屏会话。
- 新增 `ScreenReaderBridge` 单一路由：每次公告按当前正在运行的读屏进程排序，只调用 NVDA Controller Client、保益 `byctrl-x64.dll`、争渡 `ZDSRAPI_x64.dll` 中的一个；官方调用失败才保留原 `QAccessible::Focus` 事件兜底。可访问名称始终更新，真实键盘焦点、UIA 树、快捷键和游戏逻辑均未改动。
- 热切换修复：记录读屏主进程会话签名；检测到会话变化且新读屏私有接口不可用时，只在切换边界重置一次 Qt 无障碍后端再发送焦点事件。平时不重置，避免重复朗读。用户已确认运行中切换读屏问题解决。
- NVDA 按官方文档分发 2026.1.1 x64 `nvdaControllerClient.dll`，运行时动态解析 `testIfRunning`、`speakText`、`cancelSpeech`。官方压缩包 SHA-256 为 `2A3A70F729BE0C48120C5F24A81653202827C40A7986E289FFE2275A2B45011F`，DLL 为 `2FE60CF00BE929AAE32E95C1E1507A20ADA4902C8FEC273B3CC2D3BF5472932A`，LGPL 2.1 许可证原样随程序放入 `licenses`。`tools\build_release.ps1 -Package` 的安装包暂存规则已同步补齐这两个文件，避免安装后仍缺 NVDA 客户端。
- 保益按官方文档从 `C:\Program Files (x86)\BoyPcReader\byctrl-x64.dll` 加载，完成初始化、运行状态检查、独立通道朗读/停止和退出反初始化；争渡从其安装目录加载 `ZDSRAPI_x64.dll`。两者均不复制、修改或分发读屏自身 DLL。
- `tools\build_release.ps1 -Package`：全量 Configure + Release 构建 + CTest 19/19 通过，总耗时 83.03 秒，退出码 0。生成 `releases\飞船单机斗地主-Setup-1.4.exe`，61,981,930 字节，SHA-256 `B98BD3D64FC7ED4869648C6EDE45422392786C1CC70311C30E26128CA18F9BE5`，Authenticode `NotSigned`，VersionInfo 1.4.0；编译日志确认安装包已包含 NVDA DLL 与许可证。
- 构建目录游戏主程序 806,400 字节、SHA-256 `800C9D6DD8EC5E8B44BDEF46A468C742CD1132F15F1FA7BCB15DE5CD0143885B`，文件版本 1.4.0.0；更新器 72,192 字节、SHA-256 `991A86B54A9B2EFDDD88404DF76E4EAC0A121496FD24D4B6563A6C7DDAFAF2E6`，文件版本 1.4.0.0。主程序静态依赖不含 Qt6Network。
- `python tools\sign_release.py` 使用 Windows DPAPI 成功签名，密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`，发布清单为 `releases\飞船单机斗地主-Setup-1.4.release-manifest.json`。
- `python tools\publish_update.py --ssh-key <仓库外服务器私钥>` 发布成功：`windows/stable/1.4`，Release ID 66，`status=published`；脚本确认旧版本可发现 1.4、1.4 显示无更新、服务器回下载文件大小与 SHA-256 和本地安装包一致（`SERVER_VERIFICATION=OK`）。
- 覆盖安装验证通过（静默安装退出码 0）；安装后游戏、更新器文件版本均为 1.4.0.0，NVDA DLL 与许可证就位，公共桌面快捷方式仍指向安装目录游戏 EXE，用户设置与统计文件哈希保持不变。按用户“不再对软件做其它动作”的指示，未重复执行卸载或全新安装。
- 用户已确认保益、NVDA 实际试玩朗读正常；NVDA 会话中的 F11、F12、Home、左右浏览等记录均为 `backend=NVDA`。争渡运行中切换、Windows 讲述人完整人工验收和三级各 10000 局正式强度报告仍未完成，不得写成全部通过。
- 仓库更新：本提交包含读屏路由、热切换修复、安装打包清单、测试环境隔离、NVDA 官方客户端与许可登记、版本文件和文档更新；提交与标签 `v1.4` 推送到 `origin/main`。

## 2026-09-20 正式发布 1.3（首尾选牌与简洁报牌）

- 按用户指示本轮只做两件事：发布到更新服务器、更新开源仓库；产品侧唯一改动是 `version.txt` 由 1.2 升到 1.3。本机试用内核的“首尾选牌自动补牌”和顺子、连对、不带翅膀飞机的简洁报牌随 1.3 正式发布，产品代码、测试和配置本轮未再改动。
- `tools\build_release.ps1 -Package`：全量 Configure + Release 构建 + CTest 19/19 通过，总耗时 83.15 秒，退出码 0。生成 `releases/飞船单机斗地主-Setup-1.3.exe`，61,875,841 字节，SHA-256 `BDA7C6FC7420A17A535FC2C003920B403FE5F9AAE3038B9CB829B358AC83B8BE`，Authenticode `NotSigned`；安装包 VersionInfo 为 1.3.0，其中游戏与更新器文件版本均为 1.3.0.0。Release 构建的游戏主程序为 `build\release-x64\飞船单机斗地主.exe`，792,576 字节，SHA-256 `E135018ABCD13E07FB25152EB916397B5F21D975A258E21CF4406BA3BC980B92`（与本机试用内核同尺寸，仅版本资源不同）。
- `python tools\sign_release.py` 使用 Windows DPAPI 密码成功签名，固定公钥签名密钥 ID 仍为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`，发布清单为 `releases/飞船单机斗地主-Setup-1.3.release-manifest.json`。
- `python tools\publish_update.py --ssh-key <仓库外服务器私钥>` 发布成功：`windows/stable/1.3`，Release ID 65，`status=published`；脚本确认旧版本可发现 1.3、1.3 显示无更新、服务器下载文件大小与 SHA-256 与本地安装包一致（`SERVER_VERIFICATION=OK`）。服务器已按既定机制清理旧 1.2 记录与包，下一次正式发布必须从 1.4 开始。
- 依赖边界核验：Release 构建的游戏主程序不引用 Qt6Network，只有更新器引用，符合“只有独立更新器联网”的不可变边界。
- 未完成、不得写成已通过：1.3 安装包未做覆盖安装、全新安装、启动、快捷方式、卸载和用户数据保留测试（第七节第 4 步本轮由用户指示跳过）；安装目录 `C:\Program Files\飞船单机斗地主` 仍是 2026-09-20 09:54:40 换入的 1.2.0.0 本机试用内核（`5FE69D68D1AD0FD6F2D06794F753B29AC5301172A28DEBBD4C7D62B897AF9E5A`），未替换成 1.3；未做安装版实际启动试玩，也未做 NVDA、讲述人、争渡、保益人工验收。
- 仓库更新：10 个修改文件加 `version.txt` 与说明文档一并提交，创建标签 `v1.3` 并推送到 `origin/main`。

## 2026-09-20 本机试用内核：首尾选牌与简洁报牌（未发布）

- 范围：只实现“拿起顺子两端各一张、连对两端各两张时自动补齐中间点数”，以及顺子、连对、不带翅膀飞机的首尾范围报牌。自动补牌不含飞机、三带、四带、炸弹，也不做自动出牌、最长连牌搜索或提示推荐。本轮不更新版本、不打包、不签名、不提交、不推送、不发布、不联网检查更新。
- 隔离方式：在 `out\endpoint-plan-20260920-093251\work` 建立独立 git worktree（detached，基线 `fe1364bc7ad5e0757d22d226f81b2350d6ef9096`），只读复用 `Qt\6.8.3\msvc2022_64`（worktree 内用目录联接指向原 Qt），构建脚本因路径含中文自动使用 SUBST 盘符，且已确认该映射指向本 worktree，不是原仓库。原仓库在回写前保持 362 个受版本控制文件哈希全部一致、`git status` 为空。
- 产品实现：
  - `HandListModel::completeEndpointSelection(int activePlayerCount, bool allowStraight)`：只在局部候选上完成“形状判定 → 端点范围校验 → 手牌实体唯一性 → 逐点补牌 → `PatternAnalyzer::analyze` 复核”，全部通过后才用 `QSignalBlocker` 包裹 `setSelected` 提交，作用域结束后只发一次 `dataChanged(SelectedRole)`；失败路径不改变选择、拿牌顺序与单张朗读标记。
  - `MainWindow`：单张拿牌入口传 `allowStraight=true`，整组拿牌入口传 `false`（整组不凭单张首尾补顺子，但仍允许 2+2 补连对）；`putDownAllCards` 用清空前的集合判断范围文案；新增只读辅助 `conciseSelectedSequenceText`。
  - `CardTextFormatter`：新增 `formatSequenceEndpoints`，只供顺子、连对、不带翅膀的飞机使用；`AirplaneWithPairs`、`AirplaneWithSingles`、炸弹、三带、全手牌与底牌查询全部保持旧文字。
- 测试：新增确定性用例 65 个（`tests/ui/test_main_window_shortcuts.cpp` 56 个、`tests/core/test_card_text_formatter.cpp` 9 个）。针对性结果：文字目标 20 通过、UI 目标 108 通过。最终全量 `tools/build.ps1 -Preset release-x64 -Build -Test`：CTest 19/19 通过，总耗时 83.72 秒。
- 失败与修正记录：文字目标首次运行有 2 处用例自身预期写错（炸弹文字用中文“九”、把 Q/K/A 当成无效区间），已按实际既有文本修正；UI 目标修正了反向用例的端点选择、`startNewGame` 需先回到未开始阶段、以及浏览键本身也会产生一次公告导致计数基线偏早这三处测试问题。产品代码未因这些失败放宽任何断言。
- 既有用例期望更新 1 处：`testCardSelectionKeyboardCommandsAreExactAndCompatible` 中 333444 的旧文案改为“3到4飞机”，并新增“混合选择保持旧组文案”的对应用例，详见 `docs\测试与验收.md`。
- 候选产物：`work\build\release-x64\飞船单机斗地主.exe`，792,576 字节，SHA-256 `5FE69D68D1AD0FD6F2D06794F753B29AC5301172A28DEBBD4C7D62B897AF9E5A`，文件版本 1.2.0.0，依赖表不含 Qt6Network.dll，与安装目录旧主程序哈希不同。候选源文件与说明文件的哈希清单位于任务目录 `candidate\candidate-files.csv`，证据汇总在 `candidate-evidence.txt`。
- 回退办法：安装版回退用任务目录 `backup\installed\old-game.exe`（哈希等于替换前的安装版主程序）按同目录临时文件加 `File.Replace` 逐个文件恢复；源码回退按 `candidate-files.csv` 与任务目录 `backup\source` 的逐文件哈希恢复，不使用 reset/checkout/clean/stash，也不会因为回退安装版而自动抹掉本轮源码。
- 交付状态：本机试用内核已按受控回写流程写回原仓库，并于 2026-09-20 09:54:40 替换安装目录主程序。安装版主程序 SHA-256 为 `5FE69D68D1AD0FD6F2D06794F753B29AC5301172A28DEBBD4C7D62B897AF9E5A`，与冻结候选逐字节一致，文件版本仍为 1.2.0.0；替换前后安装目录均为 194 个文件，变化项只有主程序本身，更新器哈希仍为 `A32839D45794F3330D45CFB758FF08ED1F44B92C8D0A4F1644AE1D3E1A1321A3`。替换前旧主程序备份为任务目录 `backup\installed\old-game.exe`（哈希 `33851A1ABD5B2A9C3F403D88EFD631F196D65DD265E326B8CBF00F11960DE931`），需要时可单文件回退。用户桌面没有该游戏快捷方式（记录现状，本轮未创建也未修改）；公共桌面快捷方式仍指向安装目录主程序（只读核验）。本轮没有启动安装版主程序，也没有启动更新器；实际启动试玩与四种读屏人工验收仍待用户确认，不得写成已通过。

## 2026-09-20

- 删除快捷键设置对话框中的“修改所选快捷键”按钮及其焦点、成员和信号连接；功能列表继续用上下光标浏览，按回车直接进入按键选择。专项测试确认该按钮对象不存在且回车修改、单键、多选组合与冲突处理均正常。
- 正式版本升至 1.2。执行 `tools/build_release.ps1 -Package`，全量 Release 构建和 CTest 19/19 通过，总耗时 79.95 秒；生成安装包 `releases/飞船单机斗地主-Setup-1.2.exe`，大小 61,870,243 字节，SHA-256 为 `AD3166EF22826493BD43361E1FFAC0C47452D5EAEF23C455DCA039FF905FBB4B`，Authenticode 为 `NotSigned`。
- 1.2 固定公钥发布清单签名成功，密钥 ID 为 `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。完成 1.1 覆盖安装、卸载、设置和统计保留、1.2 全新安装、两个桌面快捷方式及实际启动验证；安装版游戏和更新器均为 1.2.0.0。
- 已发布更新服务器 `windows/stable/1.2`，Release ID 64；发布脚本确认服务器校验成功，1.1 可发现 1.2、1.2 无更新，下载大小、SHA-256 和固定公钥签名一致。服务器旧 1.1 发布按既定机制清理。

- 按用户实测反馈，仅修改自定义快捷键的选择方式：进入单项设置后不再直接捕获实体按键，改为用上下光标键浏览按键列表、空格键勾选或取消。单键选择一个普通键；组合键选择一个普通键并可同时勾选 Control、Shift 或 Alt，切到“使用这个热键”后按回车确认。
- 新增专项自动测试，真实模拟上下光标移动、空格勾选“上光标键”、单键替换、Control 加下翻页键多选组合、冲突拦截，并验证少按 Control 不触发、完整组合键在 Qt 与 Windows 原生消息路径均触发。`test_main_window_shortcuts` 全套通过。
- 执行 `tools/build.ps1 -Preset release-x64 -Build -Test`，Release 构建通过，CTest 19/19 通过，总耗时 80.09 秒；二人/三人/四人、AI、声音和更新签名等既有回归均继续通过。
- 仅替换安装目录游戏内核：文件版本仍为 1.1.0.0，大小 781,824 字节，SHA-256 为 `716F24C3E6C148536537CFDAC01EBE6EE42A73F649D8EFAF47BB722682B9185E`，与 Release 构建产物一致。替换前内核备份为 `artifacts/installed-kernel-backups/飞船单机斗地主-1.1-before-shortcut-key-selection-list-20260920.exe`，SHA-256 为 `96494488B23AC8CDA7B9D534EA22D8004DD360D75B688A411AE3F2A3BF9531D3`。
- 用户桌面和公共桌面的“飞船单机斗地主”快捷方式均已核验指向 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`；通过用户桌面快捷方式实际启动的进程路径及 SHA-256 均为本轮新内核，启动检查后已结束测试进程。更新器哈希仍为 `9D6F91EDD9532A361553E5B03AD9950E386233141F58C294F1117A785659E5E8`，没有打包、签名、联网、提交或推送。

- 在现有二人/三人/四人未提交试玩工作区上增加软件内全局自定义快捷键，不修改规则、AI、牌局状态、音效、更新器或版本号。旧设置没有 `shortcuts` 字段时继续使用原全部默认键。
- 菜单栏增加第 4 个顶层入口“快捷键设置”：按一下 Alt 键后可用跳格键切到该入口并按回车进入。对话框用上下光标浏览全部现有游戏快捷键，回车开始修改；先等待启动回车松开，再朗读“请按下你要设置的热键”。目标按键按下并松开只完成选择，之后跳格到“使用这个热键”并回车才确认，避免把捕获按键和确认操作混为一次事件。
- 新增稳定功能标识、默认绑定、JSON 保存、按键中文名称和一对一冲突检查。测试示例已覆盖把 F12 的“复读上一条出牌”改成“下翻页键”、旧 F12 释放以及冲突时指出已占用该键的功能。
- 自定义绑定同步接入 QAction/QShortcut、Qt event/ShortcutOverride、Windows native message、低级键盘钩子及原有 RegisterHotKey 兼容路径。跳格键和 Shift 加跳格键也可绑定：捕获状态中的第一次按下和松开用于选择，选择完成后的下一次跳格键才用于切换确认控件；普通菜单导航不变。Alt+F4 继续作为 Windows 标准关闭键。
- 执行 `tools/build.ps1 -Preset release-x64 -Build -Test`，Release 构建通过，CTest 19/19 通过，总耗时 82.45 秒；原四人中级 500 局、三人/二人各 50 局、高级 AI、农民协作、声音、更新签名和 UI 回归均继续通过。
- 仅替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`：文件版本仍为 1.1.0.0，大小 774,144 字节，SHA-256 为 `96494488B23AC8CDA7B9D534EA22D8004DD360D75B688A411AE3F2A3BF9531D3`，与 Release 构建产物一致；静态依赖不含 Qt6Network。
- 替换前二人/三人/四人版内核备份到 `artifacts/installed-kernel-backups/飞船单机斗地主-1.1-before-custom-shortcuts-20260920.exe`，SHA-256 为 `EE91729DCE109768BAB0C06304B5E6DE9C6C418643B6C9A6DA754D2CB35C6F37`。
- 更新器未替换，SHA-256 仍为 `9D6F91EDD9532A361553E5B03AD9950E386233141F58C294F1117A785659E5E8`；版本号保持 1.1，没有启动游戏或更新器、制作安装包、签名、连接服务器、提交或推送 Git。

- 按用户本机试玩要求，在未提交的三人/四人代码上继续增加可选二人模式；设置现包含“四人（两副牌）/三人（一副牌）/二人（一副牌）”，旧设置仍默认四人，进行中切换只影响下一局。
- 二人模式使用完整一副牌 54 张：双方各发 17 张、保留 3 张底牌、其余 17 张盖牌不参与本局且不进入状态、公共快照或 AI 输入；地主获得底牌后为 20 张。
- 二人局只轮转玩家一和玩家二，一方过牌即清桌；双方一输一赢且等额零和。牌型、普通炸弹/王炸翻倍与春天规则复用三人标准单副牌口径，未引入平台专属删牌或让牌机制。
- 可视牌桌把唯一电脑显示在真人正上方，第三、第四席隐藏；大键盘查询超过有效人数的席位时按当前二人/三人模式给出明确提示。原三人和四人布局、快捷键、朗读、音效、焦点及鼠标穿透路径保持。
- 新增二人发牌与 17 张盖牌隔离、叫分/出牌轮转、牌型生成、结算、设置、牌桌和 50 局中级 AI 确定性模拟测试；Release 全量回归 19/19 通过，总耗时 81.02 秒，原四人 500 局与三人 50 局继续通过。
- 仅替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`：文件版本仍为 1.1.0.0，大小 734,720 字节，SHA-256 为 `EE91729DCE109768BAB0C06304B5E6DE9C6C418643B6C9A6DA754D2CB35C6F37`，与 Release 构建产物一致；静态依赖不含 Qt6Network。
- 替换前的三人版内核备份到 `artifacts/installed-kernel-backups/飞船单机斗地主-1.1-before-two-player-mode-20260920.exe`，SHA-256 为 `1DBB9DA8EEA75D86CA51AA6520334F80E2577EB2454F9559DB203A5C4585F615`。
- 更新器未替换，SHA-256 仍为 `9D6F91EDD9532A361553E5B03AD9950E386233141F58C294F1117A785659E5E8`；版本号保持 1.1，没有启动游戏或更新器、制作安装包、签名、连接服务器、提交或推送 Git。

## 2026-09-19

- 按用户本机试玩要求增加可选三人模式，同时完整保留原四人模式：设置新增“四人（两副牌）/三人（一副牌）”选择，旧设置默认四人，设置变更从下一局生效，当前不开放二人选项。
- 三人模式采用一副牌 54 张、三家各 17 张、3 张底牌、地主 20 张；叫分和出牌只轮转三席，两家过牌后清桌，地主按两名农民单家分值的两倍结算，第四固定容量槽保持空闲和零分。
- 三人规则启用标准三带一、飞机带单、四带二单和四带两对，普通炸弹与王炸均翻倍；四人模式原有牌型限制、多级炸弹和结算完全保留，并由原 500 局模拟继续回归。
- AI 叫分底牌抽样、合法动作、深层拆牌搜索和公开牌池推断按本局人数使用 54 或 108 张牌；决策输入仍只有 `AiObservation`，没有读取其他玩家暗牌。
- 存档 schema 升至 4 并保存有效人数；旧存档缺失字段时默认四人。可视牌桌三人局隐藏第四席，F2 按模式处理 3/8 张底牌，大键盘 4 在三人局明确提示无玩家四。
- Release 重新配置和全量编译通过；新增发牌、牌型隔离、轮转、结算、存档、设置、牌桌和 50 局三人 AI 模拟回归后，最终 CTest 18/18 通过，总耗时 79.72 秒。
- 已仅替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`，文件版本仍为 1.1.0.0，大小 733,696 字节，SHA-256 为 `1DBB9DA8EEA75D86CA51AA6520334F80E2577EB2454F9559DB203A5C4585F615`，与 Release 构建产物完全一致；静态依赖复核不含 Qt6Network。
- 替换前内核备份到 `artifacts/installed-kernel-backups/飞船单机斗地主-1.1-before-three-player-mode-20260919.exe`，SHA-256 为 `EF9D4714AE81CF778CBD85E52EFE526A2E2A12187471C03B89DEB622357B6F9E`。
- 安装目录更新器未替换，SHA-256 仍为 `9D6F91EDD9532A361553E5B03AD9950E386233141F58C294F1117A785659E5E8`；本轮版本号保持 1.1，没有启动更新器、制作安装包、签名、连接或更新服务器，也没有提交或推送 Git。

- 将项目完整 Git 历史和当前 1.1 源码备份到公开 GitHub 仓库 `https://github.com/y521wan-coder/feichuan-offline-doudizhu`，默认分支为 `main`。
- 将已经发布但此前未提交的 1.1 工作区完整保存为提交 `8819c944bbf8b3964c69db51f4060ecfe143454d`，并创建注释标签 `v1.1` 精确指向该提交；四个新增源码文件均已纳入。
- 根据用户允许他人继续开发的授权加入 MIT License；本轮只处理源码开源备份，没有修改游戏功能、构建、测试、签名、打包或连接更新服务器。
- 使用 Gitleaks 8.30.1 扫描完整 Git 历史及实际上传文件，未发现凭证泄漏；安装包、构建目录、日志、用户数据和项目专用 SSH 私钥均保持排除。
- 修复 F1 开战后先暂停、再恢复时，如果当前轮到电脑，AI 计时器在暂停时停止却未在恢复后重新启动，导致牌局不再推进的问题。
- 修复仅位于 F1 的暂停/恢复状态切换路径：恢复成功并确认当前行动者不是真人时，重新调度既有 AI 回合；规则、AI 决策、快捷键、朗读、音效和界面均未改变。
- 在原有 F1 自动测试中增加断言，覆盖暂停时 AI 计时器停止、恢复时重新启动；Release CTest 17/17 通过，总耗时 77.38 秒。
- 已仅替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`，SHA-256 为 `D93F9D901E4506217F32656F3E946624B5425D0B4E8B05B3A965963761E553DE`，文件版本仍为 1.0.0.0，5 秒启动检查通过。
- 替换前内核已备份到 `artifacts/installed-kernel-backups/飞船单机斗地主-1.0-before-f1-resume-ai-20260919.exe`，备份 SHA-256 为 `3D134B9832969D5F171A607DA25DB5C9C2009FD60E138DC5B911184C9E700C98`。
- 本次没有升级版本、制作安装包、签名、连接服务器、发布更新或提交 Git；等待用户直接从当前安装版验证这一处修复。

## 2026-09-12

- 按用户试玩反馈，将地主出牌时的玩家称呼动态替换为“地主”；无论新一局地主换到哪个座位，都依据当局 `Role::Landlord` 身份判断，不写死玩家。
- 只调整出牌播报及其复读文本中的出牌者称呼；农民出牌仍读原玩家名，过牌、叫分、轮次提示、牌型语音、规则、AI、快捷键和音效均未改变。
- 针对性 UI 回归通过，覆盖真人地主、农民原名以及地主换到另一座位后的 F12 复读；Release CTest 17/17 通过，总耗时 78.66 秒。
- 已仅替换 `C:\Program Files\飞船单机斗地主\飞船单机斗地主.exe`，SHA-256 为 `3D134B9832969D5F171A607DA25DB5C9C2009FD60E138DC5B911184C9E700C98`，文件版本仍为 1.0.0.0，5 秒启动检查通过。
- 替换前内核已备份到 `artifacts/installed-kernel-backups/飞船单机斗地主-1.0-before-landlord-play-role-speech-20260912.exe`，备份 SHA-256 为 `894C161092D15AEC7B9E1F7633D164DE3604CF85E4BDF679CB9706DACB3EDB37`。
- 本次没有升级版本、制作安装包、签名、连接服务器、发布更新或提交 Git；等待用户直接从当前安装版试玩。

## 2026-09-11

- 按用户要求以当前全部试玩修改重新生成同版本 1.0 Windows x64 安装包，并复制到 `D:\飞船单机斗地主-Setup-1.0.exe`。
- 打包前重新完成 Release CTest 17/17，总耗时 78.67 秒；安装包为 61,850,525 字节，SHA-256 为 `F61BC02875B422C080760936DFC317E4FFACF950EDFA04347E6E746152FBFF43`，Authenticode 为 `NotSigned`。
- 同步生成 D 盘根目录 SHA-256 说明；没有安装该包、没有升级版本、没有签名、没有连接或更新服务器、没有提交 Git。服务器现有 1.0 发布记录仍对应 2026-08-17 的旧正式包。
- 根据用户实际试玩反馈，将整组拿起、整组放下和全部放下的牌面朗读直接改为复用左右光标浏览的 `CardTextFormatter::formatSameRankSpeech`。
- 朗读用词现完全一致：浏览读“三张三”时，拿起或放下同组也读“三张三”；四张及以上同样使用“张”而不使用“个”，对子继续沿用浏览时的“对三”。
- 只修改朗读文本格式及对应断言；单张点数朗读、选牌顺序、拿放牌音效、键盘路径、可视牌桌、规则和 AI 均未改变。
- Release CTest 17/17 通过，总耗时 78.17 秒；仅替换安装目录游戏 EXE，SHA-256 为 `894C161092D15AEC7B9E1F7633D164DE3604CF85E4BDF679CB9706DACB3EDB37`，版本仍为 1.0.0.0，5 秒启动检查通过。
- 本次没有升级版本、制作安装包、签名、连接服务器、发布更新或提交 Git；等待用户继续试玩。

## 2026-09-10

- 按用户试用要求，将出牌阶段的过牌快捷键由单独空格改为 `Ctrl+Enter`；单独空格在出牌阶段不执行操作，普通 `Enter` 仍用于出牌。
- 同步修改 Qt 键盘事件与 Windows 低级键盘钩子/native message 两条兼容路径；叫分界面与菜单的既有空格行为不变。
- 无牌可压时的提示语改为“可以按Control加回车键过牌”，并更新快捷键规格及 Qt/native 自动回归用例。
- Release 增量编译成功，CTest 17/17 通过，总耗时 54.92 秒；新内核 5 秒启动检查通过。
- 仅将 `飞船单机斗地主.exe` 替换到 `C:\Program Files\飞船单机斗地主` 供用户试用，文件版本仍为 1.0.0.0；原内核已备份到项目 `artifacts/installed-kernel-backups`。
- 未制作安装包、未修改版本号、未签名、未连接或更新服务器、未提交 Git；等待用户实际玩牌反馈后再决定后续调整。
- 根据用户第二轮试玩要求，为拿起和放下牌增加本次操作牌面的读屏反馈，同时保留原拿起与放下音效。
- `上`/`下` 每次朗读单张点数；`Ctrl+上` 和 `Ctrl+下` 按点数组朗读本次操作的牌。
- 朗读通过既有 Qt/UIA 状态公告控件发送，不移动真实键盘焦点、不改状态栏可见文字；CardSelection 类允许连续相同牌面逐次发出，选牌键原有自动重复拦截不变。
- 新增拿起、放下、整组及多组朗读自动断言；后续按用户反馈统一为与左右浏览相同的“张数+点数”格式。
- 拿放牌朗读试用内核曾因游戏正在运行而安全停止覆盖；用户关闭后已仅替换安装目录游戏 EXE，哈希校验和 5 秒启动检查通过。
- 根据用户第三轮试玩要求，退出牌局确认框的标准按钮显式改为中文“是”“否”，并将“否”设为 Enter 默认按钮和 Esc 按钮。
- 新增退出确认框中文按钮及默认“否”自动测试；第三次 Release CTest 17/17 通过，总耗时 54.19 秒。
- 核对安装脚本 `[Languages]` 当前只启用 `ChineseSimplified.isl` 简体中文语言；记录未来安装包全部安装界面必须保持中文，本轮未运行打包。
- 最终试用内核已仅替换到安装目录游戏 EXE，SHA-256 为 `F51E1F7D2A716DC0F83B584874A2A78A0AB57731BDD13FEA4F516479D511FF59`，文件版本仍为 1.0.0.0，5 秒启动检查通过。
- 经用户确认，新增程序自行绘制的经典可视牌桌，不使用外部卡牌图片或第三方素材，也不增加鼠标操作。
- 新牌桌显示真人正面手牌、已拿起牌的抬起状态、三家牌背及剩余张数、地主/农民身份、公开底牌、上一手牌、过牌信息和当前行动者高亮。
- `CardTableWidget` 只接收 `PublicGameSnapshot`、真人手牌和选中牌 ID；不持有游戏引擎或完整牌局状态，电脑暗牌无法进入该组件。
- 牌桌设为 `Qt::NoFocus` 和鼠标穿透；原手牌列表、Qt/UIA 焦点、键盘、朗读、声音、按钮及规则链路全部保留。
- 新增可视牌桌自动回归，覆盖隐藏底牌、三家牌背数量、公开牌、键盘选牌同步和鼠标点击不改变手牌。
- 完成两次实际渲染检查；首次发现真人信息条被牌面遮挡，调整后确认 25 张手牌、牌背、底牌区域、玩家信息和当前行动者均清楚可见。
- 最终 Release CTest 17/17 通过，总耗时 54.71 秒；仅替换安装目录游戏 EXE，SHA-256 为 `462EC25737374939F8AE6FA1D0FF64814AC567FC9C9BC90F60FB12E4E56D26DB`，文件版本仍为 1.0.0.0，5 秒启动检查通过。
- 本轮没有制作安装包、修改版本号、签名、连接或发布更新服务器，也没有增加任何网络依赖。
- 按用户确认将两套 AI 增强同时完成：新增有限深度束搜索，对全部合法候选先快速评估，再对最佳候选继续规划后续拆牌手数、顺子/连对/飞机结构、带牌代价、零散牌和控制牌保留。
- 新增仅基于 `AiObservation` 的公开信息推断：从本家手牌、公开出牌、公开底牌、各家剩余张数、叫分和最近过牌状态构造确定性可能牌局，估算对手压制概率和队友接牌机会；没有读取或传入任何电脑暗牌。
- 中级使用 24 个公开信息样本，高级使用 96 个；三档搜索深度、候选数和随机度边界保持原设计，初级继续保持较弱且快速。
- 新规划评分曾使“地主剩一张时最低牌守门”受到牌型分影响；已把该既有农民硬规则明确恢复为最高优先级，并通过全部 10 个农民协作场景。
- 首版深搜在批量模拟中偏慢，已收紧为先全量快评、再对 2/4/8 个最佳候选做深层束搜索；最终两组 250 局中级模拟分别为 16.11 秒和 15.08 秒，高级 500 局合法性测试为 40.69 秒。
- 新增确定性测试，覆盖紧凑顺子优于零散单牌、公开过牌信息降低推测压制风险、相同公开观察与种子结果一致，以及中/高级公开推断样本梯度。
- 最终 Release CTest 17/17 通过，总耗时 77.90 秒；仅替换安装目录游戏 EXE，SHA-256 为 `F08269E84B14B2D5D8E727FD3048FB15D7E4D84FE185F63A3A39C3FED92EF383`，文件版本仍为 1.0.0.0，5 秒启动检查通过。
- 本次仍未制作安装包、升级版本、签名、连接或发布更新服务器；用户可直接从原安装快捷方式试玩增强后的机器人。

## 2026-08-17

- 建立 `D:\飞船单机斗地主` 全新项目和 Git 历史。
- 从用户自有旧项目迁移游戏源码、测试、文档和素材。
- 排除训练中心、模型/训练产物、安装包、旧构建与参考项目代码。
- 建立规则、AI 协作、无障碍、架构、更新与许可文档。
- 开始清理旧最高难度命名、模型加载和训练通信，并修正底牌可见时机。
- 完成 Beginner/Intermediate/Advanced 内置策略档案，删除模型包、加载路径、晋级与训练通信。
- AI 决策入口改用 `AiObservation`，仅包含本家手牌和公共快照；新增农民团队策略、地主策略和公开诊断原因码。
- 叫分阶段隐藏底牌，地主确定后公开一次；修复 F2 文案和地主确定后的底牌标签显示。
- 拆出独立更新器，游戏主程序不再链接 Qt Network；加入 SHA-256 与 Authenticode 验证入口。
- 配置 Qt 6.8.3 和 MSVC 构建环境，生成 `飞船单机斗地主.exe` 与 `飞船单机斗地主更新器.exe`。
- 快速回归 14/14 通过；农民协作 8 个场景通过；UI 快捷键 39/39 通过。
- 中级 AI 引擎模拟 500/500 通过，并拆成两个 CTest 进程避免 Qt 五分钟看门狗误报；高级 AI 开发期自动牌局 500/500 通过。
- 静态扫描确认无 Master/大师、旧产品标识、模型包和排除二进制，Qt Network 仅存在于更新器边界。
- 将根目录 `继续开发说明.txt` 扩写为新会话完整接手手册，补充架构、接口、构建、测试、风险、修改流程、静态审计和推荐首条消息。
- 交接审计发现 `GameState::fromJson` 会在叫分存档含完整 8 张底牌时错误设置 `bottomCardsRevealed=true`；已记录为下一轮 P0，现有旧兼容测试的错误期待也需同步修正。
- 修复 `GameState::fromJson`：叫分和暂停叫分存档恢复后无条件隐藏底牌，同时保留内部完整底牌供地主确定后公开。
- 将错误的旧兼容测试改为 `testBiddingSaveKeepsCompleteBottomCardsHidden`，新增暂停叫分恢复、损坏存档公共快照和恢复后 AI 暗牌不变性测试。
- 功能提交：`726936a fix: keep bidding bottom cards hidden after restore`。
- Debug 快速回归 14/14 通过（8.02 秒）；完整 Advanced AI 测试设置 `QTEST_FUNCTION_TIMEOUT=900000` 后通过（395.35 秒）；完整 UI 快捷键测试通过。
- 首次直接运行 Debug CTest 因缺少缓存要求的 R: subst 映射出现 14 项 Not Run；使用临时映射后全部通过。首次未提高 QtTest 单函数超时时 Advanced 500 局在 300.008 秒触发看门狗，按项目规定提高到 900 秒后通过。
- 执行 `tools/build_release.ps1 -Deploy`，Release 编译 148/148、CTest 17/17 通过（55.48 秒），两组共 500 局模拟、Release AI、农民协作、UI 和声音测试均通过。
- 生成 `artifacts/portable-current` 绿色版，共 238 个文件、约 119 MB；主程序隐藏启动 5 秒检查通过。
- 按用户要求未执行 `-Package`、未生成安装包；桌面创建“飞船单机斗地主（绿色版测试）”快捷方式，等待用户手动测试。
- 静态扫描确认无 Master/大师、旧产品标识、模型包和禁止二进制，Qt Network 仍只存在于独立更新器及其测试边界。
- 根据用户绿色版测试反馈，取消 Alt+大键盘 1/2/3/4 玩家信息快捷键，改为单独大键盘 1/2/3/4。
- 保留真人叫分阶段 0/1/2/3 的原有优先行为；小键盘数字不作为玩家信息查询键。
- 同步修改 Qt 事件、ShortcutOverride、Windows native message、低级键盘钩子和 RegisterHotKey 路径，确保 Alt+数字不再被游戏占用。
- 新增 `testNativePlainNumberQueriesPlayersAndAltNumberIsReleased`，UI 快捷键测试增至 40 项。
- 功能提交：`cf63982 fix: move player queries to plain number keys`。
- Debug UI 快捷键 40/40 通过（4.272 秒），声音测试 12/12 通过（1.694 秒），Debug 快速回归 14/14 通过（6.60 秒）。
- Release 增量编译 99/99、CTest 17/17 通过（53.64 秒），两组 500 局模拟、AI、农民协作、UI 和声音测试均通过。
- 已覆盖生成 `artifacts/portable-current` 绿色版，主程序隐藏启动 5 秒检查通过；桌面快捷方式目标保持不变。
- 本轮仍未执行 `-Package`，未生成安装包。
- 为下一会话制作安装版，完整覆盖重写根目录 `继续开发说明.txt`，将交接目标切换为 Windows x64 安装测试版。
- 核对当前版本号为 3.0，Inno Setup 6、MSVC v145 VC++ 运行库和 Qt 6.8.3 windeployqt 均存在。
- 确认当前没有 `build/installer-stage`，`releases` 只有 `.gitkeep`，此前从未实际执行 `build_release.ps1 -Package`。
- 在交接中记录 Inno AppId、默认 Program Files 安装、管理员权限、桌面/开始菜单快捷方式、VC++ 运行库静默安装和旧文件清理行为。
- 明确当前没有真实代码签名证书；下一会话生成的未签名安装包只能用于本机安装测试，不能作为正式更新包。
- 写明安装、覆盖安装、快捷键、底牌隐藏、卸载和用户数据保留的完整验收步骤，以及安装包大小、SHA-256 和 Authenticode 状态的交接要求。

- 将产品版本线重置为 1.0，软件名称、产品标识和 Inno AppId 保持稳定。
- 为游戏和更新器加入 Windows 文件版本资源，文件版本和产品版本均为 1.0.0.0。
- 将更新安全机制改为 HTTPS、文件大小、SHA-256 与内置固定 RSA 3072 位公钥签名联合校验；主程序仍不链接 Qt Network。
- 新增更新签名测试向量和篡改拒绝测试；Debug 与最终 Release CTest 均为 17/17 通过。
- 新增免重复输入密码的发布密钥工具：私钥随机密码加密，密码仅以当前 Windows 用户 DPAPI 数据保存；旧的未发布密钥已归档且从未用于服务器发布。
- 新增 `sign_release.py` 和 `publish_update.py`，完成本地签名、SSH/SCP 上传、服务器发布、API 与下载哈希自动验证。
- 更新服务器新增固定公钥字段、发布签名字段、迁移 0005、签名验证、版本递增限制和安全暂存清理命令；服务器测试 25/25 通过。
- 生成并验证 `飞船单机斗地主-Setup-1.0.exe`：61,834,752 字节，SHA-256 为 `ECDECD550DB5D1735B53FF4131230E28450A3451C6438B7509E427FE106305F4`。
- 完成可见首次安装、静默覆盖安装、卸载、用户数据保留和最终重装验收；游戏和更新器均可启动。
- 已发布服务器 stable 1.0，Release ID 49；0.9 可发现更新，1.0 显示无更新，下载文件与本地完全一致。
- 将最终安装包和 SHA-256 校验说明复制到 D 盘根目录，供用户发送给朋友。
- 功能提交：`2032933 feat: publish signed installer update baseline`。

## 2026-09-24 AI 对战模式开发回合（未提交、未发布）

- 目标：在 2.1 冻结单机基线之上新增“纯单机版模式 / AI对战模式”启动页、独立 AI 服务进程、云模型座位、认证管理、故障暂停与脱敏统计回放；2.1 单机行为不得改变。
- 完成：首页导航与会话隔离、`requestBid`/`requestPlay` 窄接口与本地适配器、独立 `飞船斗地主AI服务.exe`（逐行 JSON IPC、DPAPI 整体加密凭据、Responses/兼容 Responses/兼容 Chat Completions 三协议、重定向阻止、头部白名单、超时与取消）、AI 对战设置与认证管理、云叫分与出牌（响应需匹配请求号/gameId/eventSequence/阶段/座位，再由 `GameEngine::execute` 复验）、故障暂停与“重试/打开AI设置/返回模式选择”、`ai_battle` 独立数据目录与脱敏统计回放摘要。
- 目录质量：动作目录按语义签名去重、确定性编号，上限 512；超限时保留合法过牌、立即出完、全部炸弹、各牌型与主体点数代表、阻止地主立即获胜与高级 AI 首选，不随机删除。
- 真实模型接入修复：DeepSeek 官方兼容 Chat Completions 下，强度映射为 快速=`reasoning_effort:none`、均衡=`high`、深入=`max`；思考模式与 JSON 对象模式互斥，只有非思考请求附 `response_format={"type":"json_object"}`；输出预算 4096（思考 8192）；动作编号接受整数或纯数字字符串并容忍 `actionId`/`action`/`id`，越界或不存在仍失败；新增 8 类安全错误码（不记录模型正文）。
- 部署修复：AI 服务与更新器各自部署 `tls\qschannelbackend.dll`，缺少该插件时 HTTPS 会误报“无法连接模型服务”。
- 测试证据：CTest 21/21（185.60 秒）；假模型二/三/四人各 10000 局，合计 30000 局、1,366,658 次云决策，非法/未结束/重复发牌/盖牌被打出/农民炸队友/错位/目录失败全为 0，四人 262 次触发 512 裁剪并全部通过保留规则核验；本地假服务 1000 次决策 P95 48 毫秒，超时误差 <250 毫秒；`deepseek-v4-flash` 快速档真实完成二/三/四人各 1 局，137 次决策零失败，平均 0.45 秒、P95 0.68～0.72 秒。
- 本地回归：二/三人各 5000 局高级 AI 通过（非法/未结束/重复发牌/盖牌被打出/农民炸队友为 0）；四人那组按用户要求停止，未完成。
- 边界：未改版本号、未打包、未签名、未发布、未推送、未打标签、未修改 `C:\Program Files\飞船斗地主`；全部改动仍在工作区未提交。
- 交接：详见 `docs\AI对战模式交接计划.md`，长期规格见 `docs\AI对战模式规格.md`。

## 2026-09-24 AI 对战模式：补跑 2.1 本地稳定性门槛（未提交、未发布）

- 目标：在 AI 对战改动叠加到 2.1 单机基线之后重跑正式候选门槛，确认 2.1 单机行为未改变；同时补齐上一轮被中断的四人精简回归。
- 门槛结果：`ai_validation_runner.exe stability` 覆盖二人/三人/四人 × 初级/中级/高级共 9 组、每组 10000 局，合计 90,000 局全部正常结束；`illegal`、`unfinished`、`duplicate_deals`、`set_aside_played`、`teammate_bombs` 全为 0，且全部组 `cloud_requests=0`、`catalog_failures=0`。证据为 `build\release-x64\local-baseline-*.txt`（9 个结果文件加 9 个空错误文件），9 组并发总墙钟 32 分 42 秒。种子：初级 300001/300002/300003、中级 400001/400002/400003、高级 500001/500002/500003。
- 四人精简回归：`stability 4 2 5000 500003` 为 5000/5000、五项全零（地主 874、农民 4126），填补此前为空的 `build\release-x64\local-stability-4p.txt`。
- 自动回归：`.\tools\build.ps1 -Preset release-x64 -Test` 为 CTest 21/21 通过、0 失败、184.43 秒（`build\release-x64\ctest-full3.txt`）。
- 文档计数订正：1.7 时代记录的“12 组 / 120,000 局”与产物不符（`artifacts/ai-validation-1.7/` 只有 9 组 = 90,000 局），已按现存证据订正为“9 组 / 90,000 局”并在原条目保留说明；三档 AI 已无 Master/大师，凑不出第 4 档。订正只涉及计数，不改变“全部正常结束、五项计数全零”的结论。
- 只读审查（未改代码）：`src\persistence\ai_battle_statistics_repository.*` 的统计与回放摘要只写人数、胜负、地主座位、倍数、各席控制方式/本地难度或模型/强度/超时、请求延迟、错误分类码和令牌用量；回放历史只写动作类型、座位、序号、叫分值和出牌张数，不含手牌、底牌、密钥或原始响应；`ai_battle\settings.json` 只保存 `credentialId`/`credentialName`，密钥仅在 `credentials.dat` 内由 Windows DPAPI 整体加密。
- 边界：未改版本号、未打包、未签名、未发布、未推送、未打标签、未修改 `C:\Program Files\飞船斗地主`；全部改动仍未提交。
