# 2026-09-25 四人规则修订与安装版试用内核交接

## 新会话先做

1. 完整阅读 `AGENTS.md`、根目录 `继续开发说明.txt`、本文、`docs/AI对战模式规格.md`。旧的 `docs/AI对战模式交接计划.md` 包含历史过程；其中的“快速档”“15 秒”“当前安装哈希”“当前进程状态”快照已经过期，以本文和现场实查为准。
2. 运行 `git -c core.quotepath=false status --short`、`git log -3 --oneline`、`git diff --stat`，保留工作区所有既有未提交改动。当前 HEAD 为 `14122b7`，2.1 冻结代码基线为 `eafc5f07e9e373a78581ec8b061651f36f74b3e5`。不要 reset、checkout、clean、擅自提交、打包、签名、发布、推送或打标签。
3. 重新核对安装目录 `C:\Program Files\飞船斗地主` 和游戏、AI 服务、更新器进程。本文记录的是本轮结束时的状态，用户随后会亲自试玩，不能把它当成新会话的实时状态。

## 用户本轮确认的四人玩法

- 四人局用两副牌。**一张小王加一张大王**一起出，叫“**双王枪毙**”，实际出 2 张牌，在大小比较中高于所有四张同点的普通“枪”，低于五张同点的“炮”。音效文件 `card_four/boy/shuangwangqiangbi.wav` 已对应此牌型。
- **两张小王加两张大王**一起出，叫“**天尊无敌**”，实际出 4 张牌，高于所有八张同点的“天炸”，是四人局最大炸弹。原有 `card_four/boy/tianzun.wav` 与特效音效已对应。
- 四人局“飞机带对子”的对子翅膀**不能与飞机三顺主体同点数**，且每对点数互不相同。例：`33333 444 55` 不能作为 `333444 + 33 + 55` 出；`333 444 55 66` 可作为飞机带两对。二、三人局规则本轮未改。
- 本轮没有改炸弹倍数：普通枪×2、双王枪毙×4、炮×4、火箭×8、导弹×16、天炸×32、天尊无敌×64。用户确认的是牌型大小与叫法，尚未要求更改计分。`docs/规则规格.md` 保留并标明这些倍数。

## 本轮实现范围

- 牌型判定和大小比较原本已正确识别双王枪毙、天尊无敌及其顺序，因此未重写这两类炸弹的判定、比较、计分或音效。修正 `src/core/text/card_text_formatter.*`、`src/core/rules/card_pattern.*`、`src/core/engine/game_engine.cpp`、`src/ui/main_window.cpp`，让四人局出牌显示、事件文字、朗读和“上一手”查询统一用新名称；二、三人局的“王炸”名称保留。
- `src/core/rules/pattern_analyzer.*` 在四人局飞机带对判定中拒绝主体点数剩余的对子。`src/ai/legal_move_generator.cpp` 同步不生成这种翅膀，云模型合法动作目录和本地 AI 都随之遵守；最终仍由 `GameEngine` 校验。
- `apps/ai_service/ai_service.cpp` 把简短的二／三／四人规则按请求 `payload.player_count` 单独放入模型提示词；四人分支明确双王枪毙、天尊无敌和飞机翅膀规则，二／三人分支使用单副牌和王炸规则。共用的胜负目标与只选 `legal_actions` 的要求保留。每回合仍是一次模型请求，无新增调用。提示词增加文字可能影响真实模型耗时，尚无授权进行新的真实模型延迟测试，需用户手动试玩确认。
- 更新 `docs/规则规格.md` 和 `assets/docs/飞船斗地主玩法说明.txt`。后者已扩写为二人、三人、四人各自的完整玩法，包含发牌、叫分、牌型、炸弹、胜负和计分；在游戏 ALT 菜单“帮助 → 玩法说明”打开，安装目录的 `docs/` 文件已同步更新。既有菜单入口不需要改代码。
- 确定性测试调整在 `tests/rules/test_pattern_analyzer.cpp`、`tests/ai/test_ai.cpp`、`tests/core/test_card_text_formatter.cpp`、`tests/core/test_card_pattern_sound_plan.cpp`、`tests/ai_service/test_ai_service.cpp`。旧文字/音效测试曾用现在禁止的同点翅膀作为合法样例，已改为合法的 `333444 + 22 + 55`。

## 验证与已知限制

- `tools/build.ps1 -Preset release-x64 -Build` 成功。
- CTest 针对性目标 `test_card_text_formatter`、`test_card_pattern_sound_plan`、`test_pattern_analyzer`、`test_pattern_comparator`、`test_game_engine`：**5/5 通过**。`test_ai.exe` 的 `testGeneratedMovesAreAlwaysLegal`、`testSingleDeckGeneratorsAddStandardAttachmentsOnlyForSingleDeckModes`、`testFourPlayerGeneratorExcludesOverlappingAirplaneWings`：**3 个用例通过**。`test_ai_service` **1/1 通过、36.40 秒**，用本地假 HTTP 服务确认三种 API 协议分别发送对应人数规则。主窗口单用例 `testHelpActionsOpenRealTxtFiles` 通过，确认“玩法说明”菜单能打开真实 TXT 文件。
- `test_main_window_shortcuts` 曾整目标运行：游戏在运行时 111 通过、3 失败；退出游戏后 112 通过、2 失败。剩余失败为 `testAnnouncementsUseTraditionalFocusEventWithoutMovingKeyboardFocus`、`testNarratorUsesStandardAnnouncementWithoutMovingKeyboardFocus`，均是系统读屏焦点事件计数为 0，未定位为本轮规则改动导致。**不能写成主窗口测试全通过**；需要后续结合机器读屏环境单独排查。用户对争渡双语音的真实听感也仍未确认。
- 本轮未发真实大模型请求，未读取 `credentials.dat`，未做完整 CTest、90,000 局本地基线或 30,000 局假模型压力。真实模型固定场景测试会耗额度，需用户另行明确授权。

## 安装版现场

- 2026-09-25 已按先前授权结束安装版游戏和 AI 服务，备份旧主程序、AI 服务及玩法说明后覆盖这三个文件。替换后逐字节 SHA-256 一致。当前无游戏、AI 服务、更新器进程；用户可直接打开安装版手动测试。
- 安装主程序 `C:\Program Files\飞船斗地主\飞船斗地主.exe`：1,063,424 字节，SHA-256 `E569051D5E6BB6CA7B781F7149E074498C8472DA59DC5239CD0EFE3D60FD717B`，与 `build/release-x64/飞船斗地主.exe` 一致。
- 安装玩法说明 `C:\Program Files\飞船斗地主\docs\飞船斗地主玩法说明.txt`：4,623 字节，SHA-256 `60336220E4DE937059CB46E1933C0365A19B43095EE1286A5263EAD0D8BC90D4`，与仓库素材一致。备份目录里保留的是本轮替换前的旧简版说明。
- 安装 AI 服务 `C:\Program Files\飞船斗地主\飞船斗地主AI服务.exe`：124,928 字节，SHA-256 `4EEC7BE8E0943659353A33720F400239D1A8F4757A0AF998BA2DB3FC64727D1D`，与构建版一致。三个旧安装文件均备份于 `build/release-x64/install-trial-backup-20260925-four-player-rules/`。这只是试用内核文件替换，不是正式安装包发布。

## 与本轮相关的既有 AI 对战状态

- AI 对战中 1 号座位是真人，其余二／三／四人局的电脑座位共用同一个云模型配置。默认玩家名为数字 1～4，自定义名优先。思考过程不显示、不播报，出牌时才按现有朗读路径提示。
- AI 对战设置已经没有“快速／均衡／深入”强度选择；共用等待上限默认 10 秒，可用组合框上下键选值或输入 1～180 秒。AI 服务按 `player_count` 发送对应人数的简短规则和共用策略提示，让模型只返回本地合法动作编号，仍只发一次请求。
- AI 对局脱敏记录在 `%APPDATA%\Feichuan\feichuan_offline_doudizhu\ai_battle\logs\ai-game-*.jsonl`。不记录凭据、提示词、原始响应或暗牌。旧会话曾由代理完成二／三／四人各一局模拟试玩，共 41 次云决策成功；这不代表本轮新规则已经过真实云模型整局验收。
- 下一步以用户对新内核的手动试玩反馈为准；若报告具体问题，先从可复现的牌面、合法动作与脱敏日志定位。不要把主窗口焦点测试失败、争渡听感或真实模型策略水平写成已通过。
