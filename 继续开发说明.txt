飞船单机斗地主——继续开发说明
更新时间：2026-09-21
项目：D:\飞船单机斗地主
仓库：https://github.com/y521wan-coder/feichuan-offline-doudizhu

一、接手

1. 先读 `AGENTS.md`、本文及 `docs\架构说明.md`、`docs\测试与验收.md`、`docs\更新协议.md`。
2. 执行 `git -c core.quotepath=false status --short`、`git log -12 --oneline`、`git diff --stat`；不得用 reset、checkout、clean 丢弃用户修改。
3. 普通开发不联网、不签名、不发布；只有用户明确要求才执行发布。

二、正式状态

- 正式版 1.6（Windows 文件版本 1.6.0.0）；更新服务器 `windows/stable/1.6`，Release ID 68，`status=published`。下次必须从 1.7 开始。
- 安装包：`releases\飞船单机斗地主-Setup-1.6.exe`，61,991,701 字节，SHA-256 `82D2D23189BA73E5400FC5B854B2C0A168593117682861949A87D319203CBBFF`，Authenticode `NotSigned`。
- 固定公钥清单：`releases\飞船单机斗地主-Setup-1.6.release-manifest.json`；密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。服务器回下载大小、哈希和签名一致（`SERVER_VERIFICATION=OK`）。D 盘根目录有同哈希安装包副本。
- 构建主程序：842,240 字节，SHA-256 `B529EE3BDC4E59794468BA6B0E478CC96AD96CA8E052224BAE7A5B08FBB8DF09`；更新器：72,192 字节，SHA-256 `9923F68FE9AA61E8DAAC33AC633B88920911214FD9B12141197DF67DB4A99011`。主程序不依赖 Qt6Network，只有更新器联网。
- 安装目录仍为 1.4；个人桌面“飞船单机斗地主-AI测试版”指向构建目录 1.6，公共桌面正式快捷方式指向安装目录。用户已实战确认 1.6 AI 增强完美；未执行 1.6 覆盖/全新安装、卸载、用户数据保留及完整四读屏联合验收，不得写成通过。

三、产品与 1.6 AI

- Windows x64、Qt Widgets、完全本地单机；二人/三人一副牌，四人两副牌。只有初级、中级、高级三档 AI。
- 1.6 只增强 AI：在原合法动作、本家拆牌和农民硬规则之后，`ImperfectInformationSearch` 仅用 `AiObservation` 的本家手牌与公共信息生成可能牌局，模拟整桌接牌、过牌、牌权和残局对抗。新增公开原因码 `strategic_pass_for_teammate_finish`。
- 真实暗牌、洗牌结果、未来信息、`GameState`/`FullGameState` 不得进入 AI；二人 17 张盖牌永不进入状态、快照或玩家样本。三档共用农民强制协作规则，差异只能来自搜索预算、深度、候选数、公开推断和安全随机度。
- 同种子 100 局角色对照（中级→高级）：二人地主 58→78、农民 42→56；三人地主 44→51、农民 56→70；四人地主 14→22、农民 86→90。最慢高级决策约 135.789 ms。三级各 10000 局正式报告仍未完成。

四、不可变边界

- 不得恢复 Master/大师、模型文件、训练、检查点、模型晋级或训练中心通信；不得新增账号、匹配、真人联机或云牌局。
- 主程序、core、ai、ui 不得链接 Qt Network；联网只属于独立更新器。
- 未完成无障碍回归前不得重做 Qt Widgets、焦点、UIA、朗读或键盘架构。现有 NVDA、保益、争渡私有接口优先，讲述人使用标准 UIA Announcement，最后才是 Qt/UIA 焦点兜底。
- 叫分期隐藏底牌；地主确定后才公开。可视牌桌只能接收公共快照、真人手牌和选中牌 ID。
- 大键盘 1/2/3/4 查询玩家；真人叫分时 0/1/2/3 优先；Alt+数字不是游戏键。快捷键规格见 `docs\快捷键规格.md`。
- 新增第三方内容须登记来源、许可证、用途和文件范围。私钥、密码、Token、DPAPI 数据、服务器配置和 SSH 密钥不得进入仓库、文档、日志或聊天。

五、构建与验证

- Qt `Qt\6.8.3\msvc2022_64`，CMake + Ninja + MSVC x64。
- 增量：`.\tools\build.ps1 -Preset release-x64 -Build -Test`
- 全量：`.\tools\build.ps1 -Preset release-x64 -Configure -Build -Test`
- 安装包：`.\tools\build_release.ps1 -Package`
- 1.6 首次完整 CTest 19/19（218.53 秒）；正式打包再次 19/19（220.15 秒）。高级 500/500 正常结束、零非法命令；农民协作 11/11 通过。用户要求发布阶段不再重复测试。
- 修改规则或 AI 必须增加只从公共观察构造的确定性测试；修改键盘/朗读须检查 Qt、native message、低级钩子、UIA 和复读路径。AI 日志不得记录暗牌。

六、以后发布

1. 版本高于服务器；修改 `version.txt`、代码、测试和文档。
2. `tools\build_release.ps1 -Package`；核验版本、大小、SHA-256、Authenticode，并按用户要求完成安装验收。
3. `python tools\sign_release.py`。
4. `python tools\publish_update.py --ssh-key <仓库外私钥>`；验证旧版发现新版、新版无更新及回下载一致。
5. 更新文档，提交、创建 `v<版本>` 标签并推送。不得重复发布同版本、绕过签名或误称固定公钥签名为 Authenticode。

详细历史见 `docs\开发日志.md`，验收证据见 `docs\测试与验收.md`，规则见 `docs\规则规格.md`。短期无计划变更。
