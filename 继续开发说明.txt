飞船单机斗地主——继续开发说明
更新时间：2026-09-23
项目：D:\飞船单机斗地主
仓库：https://github.com/y521wan-coder/feichuan-offline-doudizhu

一、接手

1. 先读 `AGENTS.md`、本文及 `docs\架构说明.md`、`docs\测试与验收.md`、`docs\更新协议.md`。
2. 执行 `git -c core.quotepath=false status --short`、`git log -12 --oneline`、`git diff --stat`；不得用 reset、checkout、clean 丢弃用户修改。
3. 普通开发不联网、不签名、不发布；只有用户明确要求才执行发布。

二、正式状态

- 正式版 1.8（Windows 文件版本 1.8.0.0）；更新服务器 `windows/stable/1.8`，Release ID 70，`status=published`。下次必须从 1.9 开始。
- 安装包：`releases\飞船单机斗地主-Setup-1.8.exe`，61,994,151 字节，SHA-256 `0E565F269418ADC1EC4497C4AB731EC230BCDC8510117E251FF0D831D830C355`，VersionInfo 1.8.0，Authenticode `NotSigned`。
- 固定公钥清单：`releases\飞船单机斗地主-Setup-1.8.release-manifest.json`；密钥 ID `898098f7c562190b1bae909422cf7b5ef09a34bc3e400110fa18cc65a77ff81a`。服务器回下载大小、SHA-256 和签名一致（`SERVER_VERIFICATION=OK`）。
- 打包构建主程序：852,992 字节，SHA-256 `8B2570B66B8AFABDB73045E8C427353F24D5FB631D695F03B7AD42A790332D1D`；更新器：72,192 字节，SHA-256 `C1105769593F0F92F5319424870B6DB31620C14AF0C8FA8CA0012501F66F38C9`，文件版本均为 1.8.0.0。主程序不依赖 Qt6Network，只有更新器联网。注意 MSVC 链接会写入时间戳，同一份源码两次构建大小相同而哈希不同，PE 哈希只能用于比对同一次构建。
- 本机安装目录 `C:\Program Files\飞船单机斗地主` 最初已由 1.4 静默覆盖安装为正式 1.7（退出码 0）；2026-09-23 又按用户要求只换入本机试用主程序。当前游戏内核 852,992 字节、文件版本 1.7.0.0、SHA-256 `DFA19DB66E53F4429B3AABC9F0B297FA7CD573007A843D98216ECD1F723F4D42`；更新器仍为 `C0785E7DB8310356917BF84A1881F8AC11F6B49D467211491228815022A101DF`。安装目录替换前后均 196 个文件，逐文件哈希比对的唯一变化是主程序；旧正式内核备份在 `artifacts\kernel-replacement-20260923\backup\installed-game-before.exe`。替换后未启动游戏或更新器，等待用户试玩。
- 本机安装目录仍是 1.7 安装内容加 2026-09-23 试用主程序；本轮未用 1.8 安装包覆盖它，也未启动正式 1.8。旧正式内核备份仍在 `artifacts\kernel-replacement-20260923\backup\installed-game-before.exe`。
- 尚未执行、不得写成通过：1.8 覆盖安装、全新安装与卸载，NVDA/讲述人/争渡/保益四种读屏软件完整人工验收，物理键盘钩子人工实测，以及公开 CA Authenticode 签名。1.8 未修改 AI，1.7 的全知 AI 验证结论继续沿用，不新增强度声明。

三、产品与 1.7 AI

- Windows x64、Qt Widgets、完全本地单机；二人/三人一副牌，四人两副牌。只有初级、中级、高级三档 AI。
- 1.7 为全知 AI：`AiObservation` 新增 `AiFullInformation`（真实整桌手牌、真实底牌、二人模式在局外的 17 张盖牌），`makeAiObservation` 逐张校验牌 ID 合法、不重复、不超过当前人数牌堆容量，二人模式另校验盖牌恰好 17 张，失败即 `available=false`，全知推演整体退出。
- 新增 `src/ai/full_information_search.h/.cpp`，作为默认整桌推演器：在复制牌桌上做带硬截止时间和节点预算的同队极大/敌队极小搜索加有限轮次推演，总剩余牌不超过 `activePlayerCount * 6 + 2` 时切换为更深的残局搜索，候选按难度裁剪，推演内部继续套用农民强制协作规则。`StandardAiPlayer(AiDifficulty, AiInformationMode)` 默认 `FullInformation`；`ImperfectInformationSearch` 保留为 `PublicInference` 显式测试模式。
- 叫分新增全知分支：有全知信息时用“本家手牌加真实底牌”的牌力减去最强真实对手牌力，替代旧的假设底牌抽样。队友只剩一张且已知确切点数时，用真实可被压过的上限喂牌，原因码 `feed_safe_single_to_one_card_teammate`；未知点数时仍用原来的低单牌规则。
- 难度参数（预算毫秒/搜索深度/候选裁剪/全知根候选/节点预算/推演轮数/安全随机度）：初级 150/1/8/2/120/4/0.35，中级 450/2/24/3/500/8/0.10，高级 1000/4/64/4/7000/20/0。团队强制规则三级完全一致。
- 真人可见范围不变：可视牌桌、朗读和公告仍只依据公共快照、真人手牌和选中牌；全知内容不写入公共快照、可视牌桌、朗读文本、回放、统计、界面文字或诊断日志。
- 正式稳定性模拟：二人/三人/四人 × 初级/中级/高级各 10000 局，共 120,000 局全部正常结束，`illegal`、`unfinished`、`duplicate_deals`、`set_aside_played`、`teammate_bombs` 全为 0；最慢单次决策 215.171 毫秒（四人高级）。原始输出在 `artifacts\ai-validation-1.7\`（该目录已加入 .gitignore，不作为提交内容）。
- 强度结论：本轮只验证合法性、可结束性与协作硬规则，未重跑同种子强度对照，不得声称 1.7 比 1.6 更强。

四、不可变边界

- 不得恢复 Master/大师、模型文件、训练、检查点、模型晋级或训练中心通信；不得新增账号、匹配、真人联机或云牌局。
- AI 仍只能经 `AiObservation` 取值，不得持有 `GameState`、`FullGameState`、洗牌种子或未来牌序；全知内容不得进入公共快照、可视牌桌、朗读、回放、统计、界面文字或诊断日志。
- 主程序、core、ai、ui 不得链接 Qt Network；联网只属于独立更新器。
- 未完成无障碍回归前不得重做 Qt Widgets、焦点、UIA、朗读或键盘架构。现有 NVDA、保益、争渡私有接口优先，讲述人使用标准 UIA Announcement，最后才是 Qt/UIA 焦点兜底。
- 叫分期底牌对真人隐藏，地主确定后才公开。可视牌桌只能接收公共快照、真人手牌和选中牌 ID。
- 大键盘 1/2/3/4 查询玩家；真人叫分时 0/1/2/3 优先；Alt+数字不是游戏键。快捷键规格见 `docs\快捷键规格.md`。
- 新增第三方内容须登记来源、许可证、用途和文件范围。私钥、密码、Token、DPAPI 数据、服务器配置和 SSH 密钥不得进入仓库、文档、日志或聊天。

五、构建与验证

- Qt `Qt\6.8.3\msvc2022_64`，CMake + Ninja + MSVC x64。
- 增量：`.\tools\build.ps1 -Preset release-x64 -Build -Test`
- 全量：`.\tools\build.ps1 -Preset release-x64 -Configure -Build -Test`
- 安装包：`.\tools\build_release.ps1 -Package`
- 1.8 修改后针对性快捷键测试 110/110（12.774 秒）；增量 CTest 19/19（160.33 秒）；正式打包全量 CTest 再次 19/19（159.92 秒）。Inno Setup 成功生成正式安装包。
- 修改规则或 AI 必须补充只经 `AiObservation` 构造的确定性测试（覆盖全知输入一致性、非法全知输入被拒绝、相同输入产生相同决策）；修改键盘或朗读须检查 Qt、native message、低级钩子、UIA 和复读路径。AI 日志不得记录暗牌。

六、以后发布

1. 版本高于服务器；修改 `version.txt`、代码、测试和文档。
2. `tools\build_release.ps1 -Package`；核验版本、大小、SHA-256、Authenticode，并按用户要求完成安装验收。
3. `python tools\sign_release.py`。
4. `python tools\publish_update.py --ssh-key <仓库外私钥>`；验证旧版发现新版、新版无更新及回下载一致。
5. 更新文档，提交、创建 `v<版本>` 标签并推送。不得重复发布同版本、绕过签名或误称固定公钥签名为 Authenticode。

详细历史见 `docs\开发日志.md`，验收证据见 `docs\测试与验收.md`，规则见 `docs\规则规格.md`。短期无计划变更。

七、2026-09-23 正式发布 1.8

- 修改文件：`src\ui\main_window.cpp`、`src\app\shortcut_settings.cpp`、`tests\ui\test_main_window_shortcuts.cpp`、`version.txt`、相关规格/验收/开发文档和本文。
- 行为修复：Shift+左/右每个点数只朗读一次；真人叫到地主后不再追加“叫分结束，开始出牌”；数字键查询地主精简为“地主，还剩 X 张牌”；地主过牌的名称公告改为“地主”，原过牌音效不变。
- 验证命令与结果：`test_main_window_shortcuts.exe` 最终 110/110 通过；`.\tools\build.ps1 -Preset release-x64 -Build -Test` 为 19/19 通过（160.33 秒）；`.\tools\build_release.ps1 -Package` 的全量 CTest 为 19/19 通过（159.92 秒）。主程序不依赖 Qt6Network。
- 安装结果：按用户明确授权强制结束运行中的安装版游戏，随后只替换主程序；196 个安装文件中只有主程序哈希变化，更新器未变。
- 正式发布：`version.txt` 升至 1.8；固定公钥清单签名成功；服务器 `windows/stable/1.8` 为 Release ID 70、`status=published`，旧版发现新版、1.8 无更新及服务器回下载大小、SHA-256、签名均通过验证。
- 开源仓库：1.8 源码、测试、文档与版本文件提交到 `origin/main`，发布标签为 `v1.8`。
- 未完成：正式 1.8 安装包的覆盖/全新安装、启动、快捷方式、卸载和用户数据保留验收，用户对正式 1.8 的实际试玩，四读屏完整人工验收及物理键盘钩子人工实测。
