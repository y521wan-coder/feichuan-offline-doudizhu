# 项目开发约束

本文件适用于本仓库全部目录。任何一轮开发只有在更新根目录 `继续开发说明.txt` 后才算完成。

## 不可变产品边界

- 产品名称为“飞船单机斗地主”，产品标识为 `feichuan_offline_doudizhu`。
- 仅提供四人、两副牌、108 张牌的本地单机游戏；不存在账号、匹配、真人联机或云牌局。
- AI 只有 `Beginner`、`Intermediate`、`Advanced` 三档，中文显示为初级、中级、高级。
- 游戏不得加载模型文件，不得包含训练、模型安装、模型晋级或训练中心通信代码。
- 农民 AI 只能读取 `AiObservation` 中的本家手牌和公共信息；不得读取其他玩家暗牌、洗牌结果或未来信息。
- 叫分期间底牌对所有玩家和 AI 隐藏；地主确定后八张底牌公开，并加入地主手牌。
- 三档 AI 使用同一套强制团队协作规则，难度只能由搜索预算、深度、候选数量、公共信息推断与安全随机程度造成。
- Windows 版保留现有 Qt Widgets、UIA 焦点事件、键盘选牌和朗读路径；未通过无障碍回归前不得重做界面。
- 游戏核心、规则、AI 与主程序不得链接 Qt Network；联网更新只能由独立更新器负责。

## 开发和验证

- 修改规则或 AI 时必须补充确定性测试；测试只能从公共观察构造 AI 输入。
- AI 诊断日志不得记录暗牌；压队友例外必须记录公开原因码。
- 不提交构建目录、模型、训练数据、检查点、候选模型、安装包、日志或测试运行产物。
- 所有开发构建、日志、报告和发布产物必须位于本项目对应目录。
- 每轮结束更新 `docs/开发日志.md` 和 `继续开发说明.txt`，写明修改文件、测试命令、结果、已知问题、下一步、最近提交及工作区状态。

## 版权和参考边界

- 基础源码、测试、文档和素材来自用户拥有完整版权的旧项目。
- 外部项目只能用于理解行为，不复制其源码、APK、资源或读取四家完整手牌的实现。
- 新增第三方内容前必须登记来源、许可证、用途和文件范围。
