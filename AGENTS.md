# 项目开发约束

本文件适用于本仓库全部目录。

## 默认任务范围

- 用户未明确要求其它事项时，每轮只修改用户指出的功能及使它生效所必需的代码和针对性测试；完成后替换安装版内核。不得顺手修改其它功能、设置、提示词、文档或用户数据，也不得擅自提交、打包、签名、发布、推送或打标签。
- 用户另行明确要求的事项按当轮要求执行；需要替换独立 AI 服务时才替换该服务。替换安装版前保留原文件备份并核验新文件与构建产物一致。

## 不可变产品边界

- 产品名称为“飞船斗地主”，产品标识仍为 `feichuan_offline_doudizhu`，以保证旧版更新和用户数据路径兼容。
- 提供可在设置中选择的二人、三人或四人本地单机游戏；二人和三人使用一副牌 54 张，四人使用两副牌 108 张，四人仍为旧设置的兼容默认值。二人局双方各发 17 张、保留 3 张底牌并将其余 17 张盖牌移出本局，也不存在账号、匹配、真人联机或云牌局。
- AI 只有 `Beginner`、`Intermediate`、`Advanced` 三档，中文显示为初级、中级、高级。
- 游戏不得加载模型文件，不得包含训练、模型安装、模型晋级或训练中心通信代码。
- 1.7 起 AI 为全知模式：`AiObservation` 同时携带本家手牌、公共信息和一份完整的 `AiFullInformation`（真实整桌手牌、真实底牌，以及二人模式在局外的 17 张盖牌）。AI 仍只能经 `AiObservation` 取值，不得持有 `GameState`、`FullGameState`、洗牌种子或未来牌序；二人/三人/四人的牌局结构与发牌结果仍不得进入 AI。
- 叫分期间底牌对真人隐藏；地主确定后二人/三人模式公开 3 张底牌、四人模式公开 8 张底牌，并加入地主手牌。全知 AI 可读取真实底牌与二人模式 17 张盖牌，但这些内容只能经 `AiFullInformation` 进入 AI，不得写入公共快照、可视牌桌、回放、统计或诊断日志；真人可见范围仍严格按公开快照。
- 三档 AI 使用同一套强制团队协作规则，难度只能由搜索预算、根候选数、节点预算、推演轮数、候选裁剪和安全随机程度造成。
- Windows 版保留现有 Qt Widgets、UIA 焦点事件、键盘选牌和朗读路径；未通过无障碍回归前不得重做界面。
- 游戏核心、规则、AI、UI 与主程序不得链接 Qt Network；联网更新只能由独立更新器负责，云模型请求只能由独立“飞船斗地主AI服务”负责。两者之外的产品目标不得链接 Qt Network。
- 启动后必须先进入模式选择页；纯单机版继续使用冻结的 2.1 行为，AI 对战使用独立牌局实例和独立数据目录。未进入 AI 对战时不得启动 AI 服务或产生模型网络请求。
- 云模型只能从本地生成的合法叫分/出牌动作目录中返回动作编号；洗牌、牌型、合法性、行动推进、计分与结算始终由本地主程序的 `GameEngine` 最终校验和执行。
- 不得实现或预留 ChatGPT 网页账号、Cookie、浏览器凭据、Codex 登录令牌或未公开网页接口；OpenAI 只能使用正式 API 密钥。

## 开发和验证

- 修改规则或 AI 时必须补充确定性测试；AI 测试一律经 `AiObservation` 构造输入，并覆盖全知输入一致性、非法全知输入被拒绝、以及相同输入产生相同决策。
- AI 诊断日志不得记录暗牌；压队友例外必须记录公开原因码。
- 不提交构建目录、模型、训练数据、检查点、候选模型、安装包、日志或测试运行产物。
- 所有开发构建、日志、报告和发布产物必须位于本项目对应目录。
- 只有用户明确要求更新文档时，才更新指定文档；记录修改文件、测试命令、结果与实际安装状态，不扩大任务范围。

## 版权和参考边界

- 基础源码、测试、文档和素材来自用户拥有完整版权的旧项目。
- 外部项目只能用于理解行为，不复制其源码、APK 或资源；1.7 全知 AI 的推演实现必须自研并保留在 `src/ai` 内。
- 新增第三方内容前必须登记来源、许可证、用途和文件范围。
